# Lumo AI V2.0

# 智能刷题练题平台完整产品设计与技术实现方案

> 文档版本：V2.0  
> 文档状态：实施基线  
> 产品形态：本地优先的 AI 学习桌面应用  
> 技术架构：Go + Vue 3 + TypeScript + Wails v3 + SQLite + Multi-Agent + RAG

---

# 目录

1. [产品战略与总体设计](#第一章-产品战略与总体设计)
2. [用户体系与应用场景](#第二章-用户体系与应用场景)
3. [系统总体架构](#第三章-系统总体架构)
4. [核心业务模块设计](#第四章-核心业务模块设计)
5. [Multi-Agent 智能体体系](#第五章-multi-agent-智能体体系)
6. [SQLite 数据库设计](#第六章-sqlite-数据库设计)
7. [前端 UI/UX 设计](#第七章-前端-uiux-设计)
8. [Go 后端工程设计](#第八章-go-后端工程设计)
9. [云端与本地同步设计](#第九章-云端与本地同步设计)
10. [AI 技术实现方案](#第十章-ai-技术实现方案)
11. [开发路线规划](#第十一章-开发路线规划)
12. [最终产品效果](#第十二章-最终产品效果)

---

# 第一章 产品战略与总体设计

## 1.1 产品定位

Lumo AI 是面向学生、备考者和自主学习者的 AI 原生智能学习平台。产品将目标设定、能力诊断、个性化练习、即时讲解、错题复习和阶段复盘组织为一个连续闭环。

产品不是简单的题库播放器，也不承诺模型生成内容绝对正确或学习成绩必然提升。AI 输出必须有来源、有边界、可追溯，确定性业务规则优先于模型判断。

## 1.2 产品愿景与设计原则

- **学习闭环**：目标决定计划，练习产生数据，数据更新能力画像，画像驱动下一次推荐。
- **本地优先**：无网络时仍可导入、练习、判分、查看错题和复习；云端与第三方模型是增强能力。
- **主动学习**：AI Tutor 默认采用苏格拉底式引导，不在用户未请求时直接展示最终答案。
- **可解释**：每个推荐显示知识点、难度、错因、间隔复习状态等主要依据。
- **可恢复**：草稿自动保存，操作幂等，数据库可备份、恢复和迁移。
- **隐私最小化**：只向外部模型发送完成任务所需的最少内容，用户可查看、导出和删除数据。

## 1.3 目标用户

| 用户 | 主要问题 | 产品价值 |
|---|---|---|
| 备考学生 | 不知道先学什么，错题反复出现 | 计划、推荐和间隔复习 |
| 自主学习者 | 资料分散，遇到问题缺少反馈 | 本地资料库与 AI Tutor |
| 编程学习者 | 主观判断代码质量困难 | 受限执行、测试和错误诊断 |
| 教师 | 组卷、批阅和分析耗时 | 后续版本提供班级和批阅能力 |

## 1.4 版本范围

### MVP 纳入范围

- 本地学生档案、学习目标和每日计划。
- Markdown、JSON、纯文本题库导入与校验。
- 单选、多选、判断、填空、简答和基础代码题。
- 练习会话、草稿保存、客观题判分、错题归档和间隔复习。
- AI Tutor、学习规划、错因诊断和基于本地资料的检索问答。
- 数据导出、导入、备份恢复和模型配置。

### 后续版本

- 教师班级、作业发布、智能组卷和班级统计。
- 管理端题库审核、模型治理和运营监控。
- 多设备云同步、账号体系和协作学习。
- OCR/PDF 批量导入、变式题生成、模拟面试和本地模型。

### MVP 明确排除

- 实时课堂、社交关系、公开题库市场。
- 未经审核的 AI 题目直接进入正式题库。
- 在桌面宿主进程中直接执行用户代码。
- 以模型回答替代标准答案、教师评价或安全审核。

## 1.5 核心用户闭环

```text
创建目标 → 能力摸底 → 生成计划 → 开始练习 → 提交答案
    → 自动判分/AI 讲解 → 错因归档 → 间隔复习 → 更新能力画像
```

## 1.6 成功指标

| 指标 | 口径 | MVP 目标 |
|---|---|---:|
| 首次激活率 | 完成目标并提交首题的用户 / 完成引导用户 | ≥ 60% |
| 计划完成率 | 当日完成任务数 / 当日计划任务数 | ≥ 50% |
| 错题复习率 | 进入复习周期且完成复习的错题 / 到期错题 | ≥ 55% |
| AI 建议采纳率 | 采纳或继续执行建议的会话 / 有建议会话 | ≥ 35% |
| 本地数据恢复成功率 | 测试备份可恢复数据集 / 全部测试备份 | 100% |
| 严重崩溃率 | 每千次启动的崩溃次数 | < 1 次 |

## 1.7 关键降级策略

- 无网络：隐藏云模型入口，保留本地题库、规则判分和历史数据。
- 模型不可用：显示明确错误，允许重试、切换 Provider 或查看标准解析。
- 题库为空：引导导入示例题库，不展示空白推荐结果。
- AI 输出不合规：丢弃结构化结果，回退到模板化反馈，不直接写入业务数据。
- 数据库升级失败：保留原文件，进入恢复模式并提供备份路径，不覆盖用户数据。

## 1.8 本章验收标准

产品评审可从目标、范围、用户任务和指标追溯到每项 MVP 功能；5 名目标用户能够完成“创建目标、做题、查看解析、复习错题”的闭环；以上四类降级流程均有可验证的 UI 状态。

---

# 第二章 用户体系与应用场景

## 2.1 角色模型

| 角色 | MVP | 数据范围 | 权限 |
|---|---|---|---|
| 游客 | 是 | 本机临时空间 | 查看示例、体验练习 |
| 本地学生 | 是 | 本机个人空间 | 管理目标、题库、答案、记忆和导出 |
| 云端学生 | 后续 | 个人云空间及已授权设备 | 同步和恢复 |
| 教师 | 后续 | 授权班级 | 布置、批阅、查看班级统计 |
| 管理员 | 后续 | 管理范围内的组织数据 | 审核、模型和权限管理 |

MVP 不要求登录。首次启动创建本地工作区，工作区使用随机 `workspace_id` 标识；账号登录后的本地数据合并必须经过用户确认。

## 2.2 权限原则

- 所有业务操作都在应用服务层进行资源归属校验。
- 前端路由隐藏不等于权限控制，Wails 方法必须独立校验身份和工作区。
- 导出、删除、恢复和模型密钥查看属于敏感操作，需要二次确认。
- 教师只能访问明确授权的班级数据，不能读取学生私密 AI 记忆。
- 管理员操作必须写入审计日志，敏感数据默认脱敏展示。

## 2.3 关键场景

### 首次启动

前置：应用首次安装。主流程：选择学习方向、输入考试日期和每日时长、导入可选题库、生成第一天任务。异常：日期无效、题库解析失败或模型不可用时，仍允许创建本地目标和手动任务。

### 完成一次练习

用户进入任务，系统创建练习会话，按顺序展示题目，持续保存答案草稿。提交后执行规则判分或排队 AI 评分，展示得分、解析、知识点和下一步复习动作。

### 离线复习

用户打开到期复习列表，系统只使用本地题目、历史解析和本地评分。需要 AI 时显示“联网后继续”，不得阻塞规则复习。

### 登录合并本地数据

先备份当前工作区，服务端返回远端摘要，按实体类型显示合并预览。用户确认后执行幂等合并；冲突内容保留本地副本和远端副本，绝不静默覆盖。

### 教师发布作业

后续版本中，教师选择题目版本、截止时间和评分规则，学生端拉取作业快照。作业发布后题目版本不可变，修改需生成新版本。

## 2.4 核心对象生命周期

- 学习目标：`draft → active → paused → completed → archived`。
- 计划任务：`planned → available → in_progress → completed | skipped`。
- 练习会话：`created → answering → submitted → graded → reviewed | abandoned`。
- 题目：`draft → reviewed → published → archived`；已发布题目不可原地修改。
- AI 会话：`created → streaming → completed | failed | cancelled`。

所有状态变化必须检查当前状态、操作者和前置条件，并生成事件记录。

## 2.5 本章验收标准

权限测试覆盖所有核心资源；游客无法读取本地学生历史数据；重复提交不会创建重复成绩；离开未提交练习时能恢复草稿；所有非法状态迁移返回稳定错误码。

---

# 第三章 系统总体架构

## 3.1 总体拓扑

```text
┌──────────────────────────────┐
│ Vue 3 + TypeScript           │
│ Router / Pinia / Renderer    │
└──────────────┬───────────────┘
               │ HTTP (POST /api/v1/{Method}) + SSE (GET /api/v1/events)
┌──────────────▼───────────────┐
│ Go Application Core          │
│ Application / Domain / Agent │
└───────┬──────────┬───────────┘
        │          │
┌───────▼──────┐ ┌─▼──────────────┐
│ SQLite       │ │ Provider Layer │
│ Files/Backup │ │ LLM/Embed/OCR  │
└──────────────┘ └──────┬──────────┘
                         │ HTTPS
                 ┌───────▼────────┐
                 │ Optional Cloud │
                 │ Sync API        │
                 └─────────────────┘
```

> **V2.0 实施基线**：传输层采用前后端分离的 Web 形态（`POST /api/v1/{Method}` 方法名式 RPC + `GET /api/v1/events` SSE 事件流 + `GET /api/v1/files` 受限下载），Go 应用服务层不依赖 HTTP；桌面端将来仅以 WebView 独立窗口渲染同一前端（或替换传输层为 Wails 绑定），业务行为与网页完全一致。

## 3.2 分层边界

- `ui`：页面、组件、状态和用户交互，不访问数据库或模型 SDK。
- `platform/wails`：只负责 DTO、绑定方法、事件转发和生命周期。
- `application`：编排用例、事务、权限和错误映射。
- `domain`：实体、值对象、状态机和不依赖基础设施的规则。
- `repository`：SQLite、文件和向量索引的抽象及实现。
- `agent`：路由、上下文、工具授权和结构化结果校验。
- `provider`：LLM、Embedding、OCR 和代码执行的可替换适配器。
- `sync`：操作日志、推送、拉取、冲突和恢复。

依赖方向只能由外层指向内层抽象。前端不得直连 SQLite，业务服务不得直接调用供应商 SDK。

## 3.3 传输契约（原 Wails 契约）

绑定方法统一返回：

```json
{
  "data": {},
  "error": null,
  "request_id": "01J..."
}
```

错误格式：

```json
{
  "code": "QUESTION_NOT_FOUND",
  "message": "题目不存在或已被删除",
  "retryable": false,
  "details": {}
}
```

方法命名采用 `资源 + 动词`，例如 `PracticeStart`、`PracticeSaveAnswer`、`PracticeSubmit`、`AgentChatStart`、`LibraryImport`。流式 AI 使用 `agent:delta`、`agent:tool`、`agent:completed`、`agent:error` 事件，事件都携带 `request_id`、`session_id` 和递增序号。

## 3.4 关键数据流

练习提交：前端提交答案 DTO → 应用服务校验会话 → 仓储事务保存提交 → 规则引擎判分 → 生成错题和复习任务 → 发布结果事件。AI 评分失败不回滚用户答案，只将评分状态置为 `pending` 或 `failed`。

资料问答：文件进入暂存区 → 计算哈希并解析 → 分块和嵌入 → 写入索引 → 检索授权片段 → LLM 生成带引用回答 → 保存摘要和调用元数据。

## 3.5 非功能要求

- SQLite 开启 WAL、外键约束和有限忙等待；写事务短小且不持有模型调用。
- 所有外部调用支持 context 取消、超时、指数退避和最大重试次数。
- 日志使用结构化字段，禁止记录 API 密钥、完整答案、完整资料和原始 Token。
- 应用启动时执行迁移、健康检查和工作区锁检查。
- 数据库备份采用一致性快照，恢复前校验版本、哈希和磁盘空间。

## 3.6 本章验收标准

架构检查确认不存在 UI 直连数据库、业务层依赖具体模型 SDK和循环依赖；断网、模型超时、迁移失败、窗口关闭取消等场景均能返回可识别错误并保持数据一致。

---

# 第四章 核心业务模块设计

## 4.1 学习目标与计划

学习目标字段包括名称、科目、考试日期、目标分数、每日学习分钟数、每周可用天数和目标知识点。Planner 根据目标、历史正确率、最近复习时间、题目难度和可用时间生成任务。

计划任务必须记录生成原因、来源目标、预计时长和优先级。用户可手动调整任务；AI 只能提出变更，不能覆盖已完成记录。

推荐排序：到期复习优先，其次是低掌握知识点、近期连续错误、目标相关性、难度匹配和题目新鲜度。每条推荐返回 `reason_codes`，例如 `REVIEW_DUE`、`LOW_MASTERY`。

## 4.2 题库与题目版本

题目统一包含：题型、题干、选项、标准答案、解析、知识点、难度、来源、标签和版本号。导入采用临时批次，逐题校验后才能发布。重复题通过内容规范化哈希和来源标识检测，导入操作使用幂等键。

题目版本不可变。编辑会创建新版本；旧版本继续被历史提交引用。AI 生成题必须标记来源、生成模型、提示词版本和审核状态。

## 4.3 练习与答题

练习会话创建时固定题目版本快照，支持暂停、恢复、跳过、返回和重新作答。答案草稿按题保存，提交时生成不可变提交记录；重新判分产生新的评分记录，不修改原始答案。

题型规则：单选必须且只能选择一个选项；多选按配置采用全选得分或部分得分；判断统一存储为单选，选项为 `A 正确 / B 错误`；填空支持空白、大小写和数值容差配置；简答进入量规评分；代码题进入隔离执行器。

## 4.4 判题与批阅

- 客观题规则引擎同步执行，结果包括得分、满分、判分规则版本和命中项。
- 简答题先执行敏感内容和长度校验，再将题目、量规和答案发送给 Grader，要求返回 JSON；低置信度结果标记为待复核。
- 代码题只接受允许的语言和文件结构，在无网络、只读文件系统、CPU/内存/进程数限制的沙箱运行，超时立即终止。
- 任何 AI 评分都展示评分依据、置信度、标准解析和申诉入口。

## 4.5 错题与间隔复习

错题记录关联题目版本、错误答案、错因、知识点和最近一次判分。错因可由规则和 Diagnoser 共同生成，分类包括概念不清、审题错误、计算错误、记忆混淆、方法不熟和表达不完整。

复习卡使用简化 SM-2 参数：`repetition`、`interval_days`、`ease_factor`、`due_at`。用户评级为“不会、模糊、掌握”，评分后更新复习间隔；复习数据只追加事件，当前卡片为投影结果。

## 4.6 模拟考试

模拟考试固定题目、顺序、时长和评分规则。考试期间自动保存，时间到期自动提交。成绩报告包含总分、分题型得分、知识点表现、耗时分布和复习建议；报告生成失败不影响成绩保存。

## 4.7 本章验收标准

覆盖空题库、重复导入、重复提交、答案恢复、时间到期、客观题边界、AI 评分失败、代码超时和错题复习到期等用例；每个状态机均有合法与非法迁移测试。

---

# 第五章 Multi-Agent 智能体体系

## 5.1 Agent 总体原则

Agent 是受约束的能力单元，不是直接写库的自主进程。所有工具调用经过 Router、权限检查、参数 Schema 校验和审计；Agent 只能通过应用服务提交建议或结构化结果。

## 5.2 Agent 能力卡

| Agent | 触发 | 输入 | 输出 | 工具 |
|---|---|---|---|---|
| Core/Router | 用户意图或系统事件 | 消息、页面、上下文 | 意图、置信度、路由 | 无 |
| Planner | 创建或调整目标 | 目标、画像、可用时间 | 计划任务列表 | 查询题目、查询复习 |
| Profiler | 完成练习或阶段复盘 | 提交、历史事件 | 知识掌握度、证据 | 查询统计 |
| Tutor | 用户请求讲解 | 题目、答案、证据 | 引导消息、下一问 | 检索、查看解析 |
| Grader | 主观题提交 | 题目、量规、答案 | 分数、理由、置信度 | 无 |
| Diagnoser | 产生错误 | 题目、答案、历史 | 错因和纠正建议 | 查询历史 |
| Librarian | 资料问答 | 用户问题、资料权限 | 片段和引用 | 向量检索 |
| OCR | 图片/PDF 导入 | 文件 | 文本、置信度 | OCR Provider |
| Variator | 用户请求变式题 | 原题、知识点 | 草稿题目 | 查询题目 |
| Auditor | 题目入库前 | 题目草稿 | 风险、质量结果 | 无 |
| Interviewer | 模拟面试 | 目标岗位、回答 | 追问、评价 | 题库查询 |
| Coach | 学习结束或低活跃 | 画像、计划 | 反馈和行动建议 | 查询统计 |

## 5.3 Router 规则

Router 输出 `intent`、`agent`、`confidence`、`required_context` 和 `risk_level`。置信度低于阈值时展示澄清问题；涉及删除、导出、发布、代码执行等高风险操作时必须交由确定性应用服务确认。

## 5.4 工具与记忆

工具采用 JSON Schema 描述，参数禁止任意 SQL、文件路径和网络地址。短期记忆只属于当前会话；长期记忆只保存经压缩、去敏和用户允许的事实；RAG 片段必须带来源文档、页码或段落位置。

## 5.5 安全护栏

- 系统指令、用户内容、检索内容和工具结果分层传递，检索文本不能改变工具权限。
- 禁止 Agent 读取其他工作区、环境变量、密钥和未授权附件。
- 对提示注入、越权请求、危险代码和敏感信息进行输入输出检测。
- 结构化结果无法通过 Schema、业务规则或置信度校验时自动降级。

## 5.6 评估指标

建立固定金标集，至少测量路由准确率、JSON 合法率、引用正确率、Tutor 不泄露答案比例、Grader 与人工评分一致性、平均首 Token 延迟和单次成本。提示词、模型和工具版本必须进入追踪记录。

## 5.7 本章验收标准

所有 Agent 能力卡齐全；模型无法绕过权限调用工具；结构化输出失败可恢复；固定数据集评测结果可重复；AI 会话可按 `request_id` 追踪到 Provider 调用和业务结果。

---

# 第六章 SQLite 数据库设计

## 6.1 存储约定

- 主键使用 UUID 或 ULID 字符串；时间统一使用 UTC ISO 8601。
- 所有可编辑表包含 `created_at`、`updated_at`、`deleted_at`、`version`。
- 所有外键开启 `ON DELETE RESTRICT` 或明确的软删除策略。
- 数据库开启 WAL、`foreign_keys=ON`、`busy_timeout`，迁移由版本表管理。
- 原始图片、PDF 和代码文件存于应用数据目录，数据库保存相对路径、大小、MIME、哈希和加密状态。

## 6.2 核心表

| 表 | 关键字段 | 说明 |
|---|---|---|
| `workspaces` | `id`, `name`, `owner_type` | 本地数据隔离空间 |
| `users` | `id`, `workspace_id`, `display_name`, `role` | 用户档案 |
| `learning_goals` | `id`, `user_id`, `exam_at`, `target_score`, `status` | 学习目标 |
| `plan_tasks` | `id`, `goal_id`, `due_at`, `duration_min`, `status`, `reason_codes` | 计划任务 |
| `questions` | `id`, `workspace_id`, `type`, `status`, `content_hash` | 题目主实体 |
| `question_versions` | `id`, `question_id`, `version_no`, `payload_json` | 不可变题目版本 |
| `knowledge_nodes` | `id`, `name`, `parent_id` | 知识点 |
| `question_knowledge` | `question_version_id`, `knowledge_id`, `weight` | 题目知识点关联 |
| `practice_sessions` | `id`, `user_id`, `mode`, `status`, `started_at` | 练习会话 |
| `submissions` | `id`, `session_id`, `question_version_id`, `answer_json` | 原始答案 |
| `grading_results` | `id`, `submission_id`, `score`, `method`, `confidence` | 判分结果 |
| `wrong_answers` | `id`, `submission_id`, `cause`, `status` | 错题记录 |
| `review_cards` | `id`, `wrong_answer_id`, `due_at`, `interval_days` | 复习卡 |
| `agent_sessions` | `id`, `user_id`, `agent`, `status` | AI 会话 |
| `agent_messages` | `id`, `session_id`, `role`, `content_ref` | AI 消息 |
| `agent_memory` | `id`, `user_id`, `memory_type`, `summary`, `consent` | 长期记忆 |
| `documents` | `id`, `workspace_id`, `path`, `sha256`, `status` | 学习资料 |
| `document_chunks` | `id`, `document_id`, `text_ref`, `embedding_version` | 检索分块 |
| `provider_calls` | `id`, `request_id`, `provider`, `model`, `tokens`, `cost` | 模型调用审计 |
| `audit_events` | `id`, `workspace_id`, `actor_id`, `action`, `payload_json` | 操作审计 |
| `sync_operations` | `operation_id`, `entity_type`, `entity_id`, `payload`, `state` | 同步队列 |
| `schema_migrations` | `version`, `applied_at`, `checksum` | 迁移记录 |

## 6.3 约束与索引

`questions(workspace_id, content_hash)` 唯一；`plan_tasks(user_id, due_at, status)`、`review_cards(user_id, due_at)`、`submissions(session_id, question_version_id)` 建索引；所有检索必须带 `workspace_id`。软删除记录不得出现在普通查询中。

## 6.4 迁移、备份与生命周期

迁移只允许向前执行，执行前创建备份并记录校验和。恢复流程为校验备份 → 停止写入 → 恢复到临时文件 → 执行兼容性检查 → 原子替换 → 重建索引。用户可导出 JSON/ZIP；删除账号或工作区时同时清理附件、索引、缓存和同步 tombstone。

## 6.5 本章验收标准

全新建库和连续版本迁移通过；外键、唯一约束、并发写入、备份恢复和异常中断测试通过；数据库中不保存明文密钥，附件路径不能跳出工作区目录。

---

# 第七章 前端 UI/UX 设计

## 7.1 信息架构

```text
引导
├── 今日 Dashboard
├── 开始练习
├── 计划
├── 错题复习
├── 题库与资料
├── AI Tutor
└── 设置与数据管理
```

## 7.2 Dashboard

首屏展示今日任务、到期复习、连续学习天数、近期正确率、薄弱知识点和一条 AI 建议。所有指标可点击进入明细；没有数据时展示导入题库或创建目标的明确行动。

## 7.3 练习页

桌面布局为“题目区 | 答题区 | AI 助手区”，窄窗口自动改为标签页。题目区支持公式、代码和图片；答题区支持草稿自动保存、计时、跳题和提交确认；AI 区显示引用、思考引导和流式状态，不遮挡题目和答案。

## 7.4 必备状态

每个页面定义加载、空数据、离线、错误、重试、保存中、保存成功、流式生成、取消和完成状态。保存失败必须保留本地草稿；网络恢复后不自动重复提交，需显示可恢复操作。

## 7.5 设计系统

- 使用统一颜色语义：主操作、成功、警告、错误、禁用和离线状态。
- 内容优先，减少装饰性卡片；重复数据使用紧凑列表和表格。
- 按钮使用图标表达撤销、重试、关闭、展开等熟悉操作，并提供可访问名称。
- 公式和代码使用独立内容区域；长文本可滚动，不能撑破布局。
- 支持键盘操作、焦点可见、语义标签和至少 WCAG AA 对比度。

## 7.6 前端状态与路由

Pinia 按 `session`、`practice`、`library`、`agent`、`settings` 拆分。服务器或本地数据进入 store 前先校验 DTO。导航守卫检查未保存答案和未完成导入；所有异步请求支持取消和请求序号，避免过期响应覆盖新状态。

## 7.7 本章验收标准

核心任务可完全用键盘完成；窄窗口不出现横向溢出；练习草稿在重启后恢复；AI 流式过程中可取消；自动无障碍扫描无关键问题，常用分辨率完成视觉回归。

---

# 第八章 Go 后端工程设计

## 8.1 工程目录

```text
lumo/
├── cmd/app
├── internal/app
├── internal/domain
├── internal/service
├── internal/repository
├── internal/agent
├── internal/provider
├── internal/platform/wails
├── internal/sync
├── internal/database/migrations
└── frontend
```

## 8.2 包职责

`domain` 只包含实体和规则；`service` 实现用例和事务；`repository` 定义并实现持久化；`provider` 隔离外部服务；`agent` 编排 AI；`platform/wails` 负责桌面适配；`sync` 只处理变更传输，不承载领域判分。

## 8.3 配置与依赖注入

配置来源优先级为默认值、配置文件、环境变量、用户设置。密钥存操作系统凭据库，配置文件只保存 Provider 名称和非敏感参数。应用启动时构建 Repository、Provider、Service 和 Agent Router，测试使用内存或 fake 实现。

## 8.4 错误与事务

领域错误包括资源不存在、状态非法、权限不足、校验失败、冲突和外部依赖失败。应用层将其映射为稳定错误码。数据库事务不得跨越模型调用；模型调用结果先校验，再在短事务内写入。

## 8.5 可观测性

每个请求携带 `request_id` 和 `workspace_id`；日志记录事件名、耗时、结果和错误码，不记录秘密与完整用户内容。指标包括启动耗时、SQLite 锁等待、练习提交耗时、Provider 延迟、Token、成本、错误率和崩溃数。

## 8.6 测试矩阵

- 单元测试：领域规则、状态机、评分、复习算法和 DTO 校验。
- 仓储集成测试：迁移、事务、索引、并发和备份恢复。
- Provider 契约测试：超时、重试、取消、限流和结构化输出。
- Wails 绑定测试：参数映射、事件序列和错误码。
- 端到端测试：引导、练习、错题、导入、AI 降级和数据导出。

## 8.7 本章验收标准

干净环境可构建；静态检查、单元和集成测试通过；架构测试禁止 UI 或业务层直接引用具体 SDK；Provider 故障能映射到前端可恢复状态。

---

# 第九章 云端与本地同步设计

## 9.1 同步原则

本地 SQLite 是事实源。MVP 提供加密导出/导入和可选云备份；多设备同步在独立里程碑实现。同步不上传整个数据库文件，而是上传版本化、幂等的变更操作。

## 9.2 操作格式

```json
{
  "operation_id": "01J...",
  "device_id": "device-uuid",
  "workspace_id": "workspace-uuid",
  "entity_type": "review_card",
  "entity_id": "entity-uuid",
  "base_version": 3,
  "operation": "update",
  "payload": {},
  "created_at": "2026-08-04T00:00:00Z"
}
```

服务器以 `operation_id` 幂等；客户端按本地队列顺序推送，成功后记录服务端序列号，再拉取游标之后的变更。

## 9.3 冲突策略

- 计划标题、标签等低风险字段：字段级合并。
- 用户偏好：末写优先，但保留审计记录。
- 题目版本和标准答案：不可合并，生成冲突副本。
- 答案、评分、学习事件：只追加，禁止覆盖历史事实。
- 删除：使用 tombstone，恢复操作产生新版本。

## 9.4 安全与恢复

使用短期访问令牌、刷新令牌和设备撤销；传输使用 HTTPS，敏感备份在本地先加密。首次同步展示摘要和冲突数量；账号切换前自动备份；同步失败保留队列并采用退避重试。用户可下载诊断包，诊断包默认脱敏。

## 9.5 云端接口

核心接口为 `POST /v1/devices`、`POST /v1/sync/push`、`GET /v1/sync/pull?cursor=`、`POST /v1/backups` 和 `DELETE /v1/workspaces/{id}`。接口必须支持幂等键、分页、游标、错误码和服务端时间。

## 9.6 本章验收标准

双设备离线编辑、重复投递、网络中断、时钟漂移、删除后恢复和服务端返回 409 均通过；任何同步故障都不丢失本地数据，并能显示待处理数量和恢复动作。

---

# 第十章 AI 技术实现方案

## 10.1 Provider 抽象

定义 `LLMProvider`、`EmbeddingProvider`、`OCRProvider` 和 `CodeRunner` 接口。接口包含模型名、超时、取消、流式回调、结构化 Schema、Token 用量、费用估算和错误分类。业务层只依赖接口，不依赖供应商类型。

## 10.2 RAG 管线

```text
文件校验 → 哈希去重 → 文本解析/OCR → 清洗
→ 按标题与语义分块 → Embedding → 索引
→ 权限过滤 → 混合检索 → 重排 → 引用拼装 → LLM 回答
```

支持 Markdown、TXT、PDF 和图片。分块保留文档、章节、页码、段落和偏移元数据；Embedding 版本变化时建立新索引，完成后原子切换。检索默认结合关键词和向量，Top-K 结果经过权限过滤和去重。

## 10.3 回答约束

Tutor 需要区分题目事实、资料引用和推理建议。无足够证据时明确说明不确定性，不编造来源。引用展示文档名称、位置和匹配片段。涉及标准答案时优先使用题目版本中的人工或审核内容。

## 10.4 记忆系统

短期记忆保存当前题目、用户回答和最近对话；长期记忆只保存“常错知识点、偏好讲解方式、学习时间习惯”等摘要。记忆写入需满足去敏、去重、用户授权和可删除条件；原始长对话不默认永久保存。

## 10.5 可靠性与成本治理

单次调用设置连接超时、总超时、最大输出 Token、重试次数和熔断阈值。按用户和工作区设置每日配额；超额后切换小模型、使用缓存或回退模板。缓存键包含模型、提示词版本、输入哈希和资料版本，避免旧答案污染新题目。

## 10.6 隐私与安全

敏感内容分类为身份信息、学习内容、凭据和高风险代码。发送前按用户设置脱敏；日志只保存哈希、长度、Token 和错误信息。支持关闭云模型、使用本地模型、删除资料索引和删除长期记忆。模型服务商的留存政策必须在设置页明确展示。

## 10.7 AI 评测

评测集覆盖公式、长资料、OCR 噪声、相似题、错误答案、提示注入和无证据问题。指标包括 Recall@K、MRR、引用准确率、答案忠实度、Tutor 引导合规率、评分一致性、P95 延迟和平均成本。每次模型或提示词变更必须重新评测并记录版本。

## 10.8 本章验收标准

资料问答可定位引用；无证据问题不会伪造答案；Provider 超时可取消并重试；敏感信息不进入日志；固定评测集结果可重复且低于阈值时阻止发布。

---

# 第十一章 开发路线规划

> **V2.0 实施状态（2026-08）**：P0（基础骨架）、P1（本地练习闭环）、P2（AI 辅导）、P3（资料导入与 RAG）、P4（备份与同步骨架）均已按本路线完成实现并通过端到端冒烟（`scripts/smoke.ps1` 15 项全过）；P5（教师端与跨平台发布）为后续里程碑。同步当前为本地模拟实现（sim-server 文件存储），云端真实部署沿用既有操作协议。

## 11.1 P0 基础骨架

交付：Wails 桌面壳、Vue 路由、Go 分层、SQLite 迁移、工作区、配置和错误模型。退出标准：干净环境构建成功，创建工作区并完成备份恢复。

## 11.2 P1 本地练习闭环

交付：题库导入、题目版本、练习会话、草稿、客观题判分、错题和复习卡。退出标准：核心端到端测试通过，断网可完整练习，重复提交和异常退出不丢数据。

## 11.3 P2 AI 辅导与能力画像

交付：Router、Planner、Profiler、Tutor、Grader、Diagnoser、Provider 抽象和成本记录。退出标准：结构化输出校验、取消、重试、安全测试和金标评测通过。

## 11.4 P3 资料导入与 RAG

交付：PDF/OCR、分块、Embedding、混合检索、引用和索引管理。退出标准：评测集达到召回、引用和延迟阈值，资料权限隔离有效。

## 11.5 P4 备份与云同步

交付：加密导出、备份、设备注册、操作日志、拉推、冲突副本和恢复界面。退出标准：双设备故障注入测试通过，无本地数据丢失。

## 11.6 P5 教师端与跨平台发布

交付：班级、作业、智能组卷、AI 批阅、Windows/macOS/Linux 安装包。退出标准：权限隔离、签名、自动更新、回滚和平台兼容性测试通过。

## 11.7 质量闸门

- 功能：P0 缺陷为零，核心流程有自动化测试。
- 数据：迁移、备份、恢复和删除流程通过演练。
- 安全：密钥、路径、代码执行、提示注入和越权测试通过。
- 性能：启动、页面响应、数据库查询和 AI 延迟达到目标。
- 体验：核心任务可用性测试通过，键盘与无障碍检查无关键问题。
- 发布：版本、迁移、依赖、许可证、隐私政策和回滚包齐全。

## 11.8 主要风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 模型回答不稳定 | 错误学习反馈 | 结构化输出、引用、评测和人工复核 |
| Provider 限流或涨价 | AI 不可用 | 配额、缓存、多 Provider、模板降级 |
| 代码执行逃逸 | 严重安全事件 | 独立沙箱、无网络、资源限制和审计 |
| 数据迁移失败 | 用户数据损失 | 自动备份、临时恢复、校验和和回滚 |
| Wails 平台差异 | 发布延期 | 尽早构建三平台 CI 和安装测试 |

---

# 第十二章 最终产品效果

## 12.1 MVP 验收故事

1. 新用户首次启动，在无账号、无网络状态下创建目标并导入题库。
2. 用户完成一组选择题，关闭应用后重新打开，答案草稿和会话状态正确恢复。
3. 提交后立即看到客观题得分、解析、知识点和错题归档。
4. 到期复习自动出现在 Dashboard，用户可离线完成复习并更新间隔。
5. 用户请求 AI Tutor 时看到流式引导；取消请求后页面可继续使用。
6. 用户从本地资料提问，回答包含准确引用；无证据时明确提示。
7. 用户导出加密备份，在新工作区恢复后题目、记录、计划和设置完整。

## 12.2 性能目标

| 项目 | 目标 |
|---|---:|
| 冷启动到可交互 | ≤ 3 秒（常规开发机） |
| 本地页面操作反馈 | P95 ≤ 100 ms |
| 本地题目提交 | P95 ≤ 300 ms，不含 AI |
| SQLite 常用查询 | P95 ≤ 100 ms |
| AI 首 Token | P95 ≤ 5 秒，受 Provider 影响 |
| 练习草稿恢复 | 重启后 100% 恢复最近一次已确认保存 |
| 数据恢复 | 测试备份恢复成功率 100% |

## 12.3 安全与合规标准

- 密钥进入系统凭据库，不进入源码、配置导出、日志或崩溃报告。
- 用户可查看、导出、删除个人数据和长期记忆。
- 第三方 SDK、字体、模型和题库内容保留许可证及来源记录。
- AI 页面展示生成内容提示、资料来源和人工复核边界。
- 发布包进行签名，自动更新支持失败回滚。

## 12.4 发布阻断条件

出现以下任一情况不得发布：核心数据丢失或恢复失败；存在未修复的越权、密钥泄露、路径穿越或代码执行逃逸；题目版本和提交无法追溯；AI 结构化结果可直接绕过业务校验写库；核心流程存在 P0/P1 阻断缺陷。

## 12.5 最终产品定义

Lumo AI 的完成状态不是“拥有十二个 Agent”或“拥有大量页面”，而是用户可以在本地稳定完成以下闭环：

```text
学习目标 → 能力诊断 → 个性计划 → 有依据的练习推荐
→ 稳定答题与判分 → 可解释反馈 → 错题复习
→ 能力画像更新 → 下一轮学习计划
```

当本地功能、AI 辅导、资料检索、数据恢复和安全边界均达到本章标准后，学生 MVP 才具备发布条件；教师、管理、多设备同步和跨平台能力按前述里程碑逐步扩展。

---

# 附录 A：统一错误码

| 错误码 | 含义 | 是否可重试 |
|---|---|---|
| `INVALID_ARGUMENT` | 参数校验失败 | 否 |
| `UNAUTHORIZED` | 未登录或凭据失效 | 登录后 |
| `FORBIDDEN` | 无权限 | 否 |
| `NOT_FOUND` | 资源不存在 | 否 |
| `CONFLICT` | 版本或同步冲突 | 处理冲突后 |
| `INVALID_STATE` | 状态迁移非法 | 否 |
| `DATABASE_UNAVAILABLE` | 数据库不可用 | 是 |
| `PROVIDER_TIMEOUT` | 外部模型超时 | 是 |
| `PROVIDER_RATE_LIMITED` | 外部模型限流 | 延迟后 |
| `OUTPUT_INVALID` | 模型输出不符合 Schema | 是 |
| `IMPORT_FAILED` | 资料或题库导入失败 | 修正后 |
| `SANDBOX_LIMIT` | 代码执行超限 | 修改代码后 |
| `REQUEST_CANCELLED` | 流式请求被用户取消 | 重新发起 |

> 补充：Web 传输层前端侧错误标记 `NETWORK`（无法连接本地服务）与 `STREAM_FAILED`（SSE 中断），`retryable` 均为 true。

# 附录 B：文档完成标准

- 每章包含目标、范围、流程、数据或接口契约、失败处理、非功能约束和验收标准。
- 每项 AI 能力都明确输入来源、工具权限、结构化输出、成本预算、延迟预算、评测集和降级路径。
- 每项同步能力都明确事实源、操作粒度、幂等性、冲突策略、隐私权限和恢复步骤。
- 每项 MVP 承诺都能关联到用户任务流、自动化测试或人工验收用例。
- 所有外部依赖均可替换、可禁用，并且不会阻塞本地基础学习闭环。
