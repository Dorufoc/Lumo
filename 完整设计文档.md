# Lumo AI V2.0

# 智能刷题练题平台完整产品设计与技术实现方案

> 文档版本：V2.1（规划补全版）  
> 文档状态：实施基线 + 规划补全  
> 产品形态：本地优先的 AI 学习桌面应用  
> 技术架构：Go + Vue 3 + TypeScript + Wails v3（当前 Web 形态实现）+ SQLite + Multi-Agent + RAG  
> 版本说明：V2.1 在 V2.0 实施基线基础上，依据《设计文档补充大纲》落实全部待办任务，并新增学生端、教师端、管理端与开放能力等 18 个业务模块的需求详设（4.8–4.25）。标有“✅ 已实现”的需求为 P0–P4 已落地能力；标有“🔜 后续版本”的需求为规划内容，供 P5 及以后里程碑实施。

---

# 目录

1. [产品战略与总体设计](#第一章-产品战略与总体设计)
2. [用户体系与应用场景](#第二章-用户体系与应用场景)
3. [系统总体架构](#第三章-系统总体架构)
4. [核心业务模块设计](#第四章-核心业务模块设计)（含 4.8–4.25 新增模块）
5. [Multi-Agent 智能体体系](#第五章-multi-agent-智能体体系)（含扩展 Agent）
6. [SQLite 数据库设计](#第六章-sqlite-数据库设计)（含扩展表）
7. [前端 UI/UX 设计](#第七章-前端-uiux-设计)（含扩展页面）
8. [Go 后端工程设计](#第八章-go-后端工程设计)（含扩展服务）
9. [云端与本地同步设计](#第九章-云端与本地同步设计)
10. [AI 技术实现方案](#第十章-ai-技术实现方案)（含扩展 AI 能力）
11. [开发路线规划](#第十一章-开发路线规划)（含 P5–P8 详案与优先级矩阵）
12. [最终产品效果](#第十二章-最终产品效果)（含扩展验收故事）
13. 附录 A：统一错误码（含扩展）
14. 附录 B：文档完成标准

---

# 第一章 产品战略与总体设计

## 1.1 产品定位

Lumo AI 是面向学生、备考者和自主学习者的 AI 原生智能学习平台。产品将目标设定、能力诊断、个性化练习、即时讲解、错题复习和阶段复盘组织为一个连续闭环。

产品不是简单的题库播放器，也不承诺模型生成内容绝对正确或学习成绩必然提升。AI 输出必须有来源、有边界、可追溯，确定性业务规则优先于模型判断。

## 1.2 产品愿景与设计原则

- **学习闭环**：目标决定计划，练习产生数据，数据更新能力画像，画像驱动下一次推荐。
- **本地优先**：无网络时仍可导入、练习、判分、查看错题和复习；云端与第三方模型是增强能力。
- **主动学习**：AI Tutor 默认采用苏格拉底式引导，不在用户未请求时直接展示最终答案。
- **可解释**：每个推荐显示知识点、难度、错因、间隔复习状态等主要依据。
- **可恢复**：草稿自动保存，操作幂等，数据库可备份、恢复和迁移。
- **隐私最小化**：只向外部模型发送完成任务所需的最少内容，用户可查看、导出和删除数据。

## 1.3 目标用户

| 用户 | 主要问题 | 产品价值 |
|---|---|---|
| 备考学生 | 不知道先学什么，错题反复出现 | 计划、推荐和间隔复习 |
| 自主学习者 | 资料分散，遇到问题缺少反馈 | 本地资料库与 AI Tutor |
| 编程学习者 | 主观判断代码质量困难 | 受限执行、测试和错误诊断 |
| 教师 | 组卷、批阅和分析耗时 | 后续版本提供班级和批阅能力 |

### 用户画像细分

| 细分人群 | 典型特征 | 核心诉求 | 产品侧重点 |
|---|---|---|---|
| K12 学生 | 有明确考试节点、依赖家长监督 | 提分、错题不反复、家长可查看进度 | 目标计划、错题复习、家长报告 |
| 大学生 | 科目多、自主安排、期末集中备考 | 效率、覆盖广度、资料整理 | 资料导入、RAG 问答、组卷模拟 |
| 考研/考公/考证人群 | 长周期备考、竞争激烈、资料海量 | 阶段规划、真题练习、复盘 | 长周期计划、模拟考试、报告 |
| 编程学习者 | 偏实战、需要即时反馈 | 代码题诊断、执行验证 | 代码沙箱、Debugger、项目练习 |
| 语言学习者 | 听说读写并重、需要重复记忆 | 词汇记忆、口语练习、听力 | 闪卡、TTS 朗读、口语评测 |
| 自主学习者 | 无考试压力、兴趣驱动 | 知识整理、随时答疑 | 资料库、AI Tutor、知识图谱 |
| 家长/监护人 | 不直接学习、关心进度与效果 | 了解学习情况、设置使用限制 | 家长报告、使用时长、限制设置 |

产品不承诺“提分”结果，而是保证“过程可追踪、反馈可解释、数据可恢复”。

## 1.4 版本范围

### MVP 纳入范围

> **V2.0 实施状态（2026-08）**：以下 MVP 能力已全部实现并通过端到端冒烟（`scripts/smoke.ps1`），标 ✅。

- ✅ 本地学生档案、学习目标和每日计划。
- ✅ Markdown、JSON、纯文本题库导入与校验。
- ✅ 单选、多选、判断、填空、简答和基础代码题。
- ✅ 练习会话、草稿保存、客观题判分、错题归档和间隔复习。
- ✅ AI Tutor、学习规划、错因诊断和基于本地资料的检索问答。
- ✅ 数据导出、导入、备份恢复和模型配置。

### 后续版本

> 以下能力按里程碑逐步交付，详设见第四章对应小节。

- 🔜 教师班级、作业发布、智能组卷和班级统计（P5，见 4.22）。
- 🔜 管理端题库审核、模型治理和运营监控（P6，见 4.23）。
- 🔜 多设备云同步、账号体系和协作学习（P4 骨架已落地，真实云端见第九章）。
- 🔜 OCR/PDF 批量导入、变式题生成、模拟面试和本地模型（P3 已支持 PDF/图片基础管线，增强见 10.9）。
- 🔜 学习笔记与标注、闪卡记忆强化、模拟考试组卷、成就激励（见 4.8–4.11）。
- 🔜 专注计时、提醒通知、学习日历、健康辅助（见 4.13–4.17）。
- 🔜 开放能力：插件、Webhook、扩展导入导出格式、CLI（见 4.24）。

### MVP 明确排除

- 实时课堂、社交关系、公开题库市场。
- 未经审核的 AI 题目直接进入正式题库。
- 在桌面宿主进程中直接执行用户代码。
- 以模型回答替代标准答案、教师评价或安全审核。
- 以“AI 生成内容量”或“Agent 数量”作为完成标准。

## 1.5 核心用户闭环

```text
创建目标 → 能力摸底 → 生成计划 → 开始练习 → 提交答案
    → 自动判分/AI 讲解 → 错因归档 → 间隔复习 → 更新能力画像
```

## 1.6 成功指标

| 指标 | 口径 | MVP 目标 |
|---|---|---:|
| 首次激活率 | 完成目标并提交首题的用户 / 完成引导用户 | ≥ 60% |
| 计划完成率 | 当日完成任务数 / 当日计划任务数 | ≥ 50% |
| 错题复习率 | 进入复习周期且完成复习的错题 / 到期错题 | ≥ 55% |
| AI 建议采纳率 | 采纳或继续执行建议的会话 / 有建议会话 | ≥ 35% |
| 本地数据恢复成功率 | 测试备份可恢复数据集 / 全部测试备份 | 100% |
| 严重崩溃率 | 每千次启动的崩溃次数 | < 1 次 |

## 1.7 关键降级策略

- 无网络：隐藏云模型入口，保留本地题库、规则判分和历史数据。
- 模型不可用：显示明确错误，允许重试、切换 Provider 或查看标准解析。
- 题库为空：引导导入示例题库，不展示空白推荐结果。
- AI 输出不合规：丢弃结构化结果，回退到模板化反馈，不直接写入业务数据。
- 数据库升级失败：保留原文件，进入恢复模式并提供备份路径，不覆盖用户数据。

## 1.8 本章验收标准

产品评审可从目标、范围、用户任务和指标追溯到每项 MVP 功能；5 名目标用户能够完成“创建目标、做题、查看解析、复习错题”的闭环；以上四类降级流程均有可验证的 UI 状态。

## 1.9 需求全景矩阵（总纲）

> 本矩阵是全部需求的索引。每行对应一个需求方向，详设所在章节见“详设位置”。✅ = P0–P4 已实现；🔜 = 后续版本规划；两者兼顾表示基础已实现、增强待规划。

### A. 学习闭环核心（学生端）

| 编号 | 需求方向 | 版本 | 详设位置 |
|---|---|---|---|
| A1 | 工作区、用户档案与本地数据隔离 | ✅ | 4.1 / 2.1 |
| A2 | 学习目标创建、状态机与目标拆解 | ✅+🔜 | 4.1 / 4.16 |
| A3 | 每日计划生成、手动调整与推荐解释 | ✅+🔜 | 4.1 / 4.16 |
| A4 | 题库导入（md/json/text）与幂等去重 | ✅ | 4.2 |
| A5 | 题目版本、审核状态与不可变历史 | ✅ | 4.2 |
| A6 | 练习会话、草稿、跳过、幂等提交 | ✅ | 4.3 |
| A7 | 客观题规则判分与多选容差 | ✅ | 4.4 |
| A8 | 简答量规评分与代码沙箱评分 | ✅ | 4.4 |
| A9 | 错题归档、错因分析与间隔复习 | ✅ | 4.5 |
| A10 | 模拟考试与定时提交 | ✅ 基础 | 4.6 / 4.10 |
| A11 | Dashboard 统计与薄弱点提示 | ✅ | 4.12 |

### B. 学习体验增强（学生端新增）

| 编号 | 需求方向 | 版本 | 详设位置 |
|---|---|---|---|
| B1 | 学习笔记、题目批注与资料高亮 | 🔜 | 4.8 |
| B2 | 闪卡生成、评级复习与 Anki 互通 | 🔜 | 4.9 |
| B3 | 手动/自动组卷、套卷管理与考试模式 | 🔜 | 4.10 |
| B4 | 打卡、连续天数、徽章与激励规则 | 🔜 | 4.11 |
| B5 | 学习报告（日报/周报/月报）与导出 | 🔜 | 4.12 |
| B6 | 专注计时、番茄钟与时间统计 | 🔜 | 4.13 |
| B7 | 提醒通知中心与桌面通知 | 🔜 | 4.14 |
| B8 | 收藏、稍后读与资料 AI 摘要 | 🔜 | 4.15 |
| B9 | 学习日历与里程碑拆分 | 🔜 | 4.16 |
| B10 | 健康辅助（久坐/护眼/夜间/休息引导） | 🔜 | 4.17 |
| B11 | 语音朗读、听力与口语评测 | 🔜 | 4.18 |
| B12 | 知识图谱与能力画像可视化 | 🔜 | 4.19 |
| B13 | 内容创作、题目请求与分享 | 🔜 | 4.20 |
| B14 | 家长/监护人模式 | 🔜 | 4.21 |
| B15 | 学习体验补充能力（生词本/每日一题/打印/快捷键/无痕模式等） | 🔜 | 4.25 |

### C. AI 能力

| 编号 | 需求方向 | 版本 | 详设位置 |
|---|---|---|---|
| C1 | 多 Provider 抽象（LLM/Embedding/OCR/CodeRunner） | ✅ | 10.1 |
| C2 | RAG 资料问答与引用 | ✅ | 10.2 |
| C3 | AI Tutor 苏格拉底式引导 | ✅ | 5.2 / 10.3 |
| C4 | 错因诊断与学习规划 | ✅ | 5.2 |
| C5 | 主观题 AI 评分与低置信度复核 | ✅ | 4.4 / 5.2 |
| C6 | 资料摘要、要点提取与题目生成 | 🔜 | 10.9 / 10.10 |
| C7 | 变式题生成、模拟面试、口语评测 | 🔜 | 10.14 / 10.11 |
| C8 | 代码调试助手与作文批改 | 🔜 | 10.12 |
| C9 | 本地模型（Ollama 等）与多模态 | 🔜 | 10.13 |
| C10 | 记忆系统（短期/长期/可删除） | ✅ 基础 | 10.4 |

### D. 数据、安全与合规

| 编号 | 需求方向 | 版本 | 详设位置 |
|---|---|---|---|
| D1 | 加密备份与恢复（AES-256-GCM） | ✅ | 6.4 / 9.x |
| D2 | JSON/ZIP 导出与格式矩阵 | ✅+🔜 | 9.1 / 4.24 |
| D3 | 密钥本地存储与不可回读 | ✅ | 8.3 / 10.6 |
| D4 | 审计日志与敏感操作确认 | ✅+🔜 | 2.2 / 4.23 |
| D5 | 数据删除权、保留期与导出权 | ✅+🔜 | 10.6 / 6.4 |
| D6 | 诊断包与脱敏导出 | 🔜 | 9.4 / 8.5 |
| D7 | 数据迁移、版本升级与回滚 | ✅ | 6.4 |

### E. 协同与组织（教师/管理/开放）

| 编号 | 需求方向 | 版本 | 详设位置 |
|---|---|---|---|
| E1 | 班级、学生管理与作业发布 | 🔜 | 4.22 |
| E2 | 智能组卷、AI 批阅与班级统计 | 🔜 | 4.22 |
| E3 | 管理端题库审核与内容治理 | 🔜 | 4.23 |
| E4 | 模型治理、用户治理与运营监控 | 🔜 | 4.23 |
| E5 | 多设备云同步与冲突处理 | ✅ 骨架+🔜 | 第九章 |
| E6 | 插件系统与 Webhook | 🔜 | 4.24 |
| E7 | 国际化、多语言与无障碍 | 🔜 | 4.24 / 7.x |
| E8 | 移动端与多端形态 | 🔜 | 11.9.5 |

### F. 质量与工程

| 编号 | 需求方向 | 版本 | 详设位置 |
|---|---|---|---|
| F1 | 可观测性（日志/指标/追踪） | ✅ 基础+🔜 | 8.5 / 9.4 |
| F2 | 测试矩阵与质量闸门 | ✅ | 8.6 / 11.7 |
| F3 | 性能目标与容量基线 | ✅ | 12.2 |
| F4 | 发布工程（签名/自动更新/回滚） | 🔜 | 11.9.2 |
| F5 | 崩溃报告与故障恢复 | 🔜 | 11.9.2 / 12.4 |

## 1.10 本章补充验收标准（V2.1）

- 需求全景矩阵中的每一行都能在本文档中找到对应详设小节与验收标准。
- 新增模块（B1–B14、C6–C9、E1–E8）均有：定位、功能清单、规则约束、数据模型、交互要求、失败处理与验收标准。
- “已实现”标注与实际代码能力一致（以 `scripts/smoke.ps1` 与 `go test ./...` 为准）。

---

# 第二章 用户体系与应用场景

## 2.1 角色模型

| 角色 | MVP | 数据范围 | 权限 |
|---|---|---|---|
| 游客 | 是 | 本机临时空间 | 查看示例、体验练习 |
| 本地学生 | 是 | 本机个人空间 | 管理目标、题库、答案、记忆和导出 |
| 家长/监护人 | 后续 | 被监护人工作区的授权子集 | 查看报告、设置使用限制，不读私密 AI 记忆 |
| 云端学生 | 后续 | 个人云空间及已授权设备 | 同步和恢复 |
| 教师 | 后续 | 授权班级 | 布置、批阅、查看班级统计 |
| 管理员 | 后续 | 管理范围内的组织数据 | 审核、模型和权限管理 |

MVP 不要求登录。首次启动创建本地工作区，工作区使用随机 `workspace_id` 标识；账号登录后的本地数据合并必须经过用户确认。

### 角色升级路径

- 游客 → 本地学生：创建本地工作区即完成，保留引导期产生的示例数据（可一键清除）。
- 本地学生 → 云端学生：登录云账号，先备份、再预览合并摘要、确认后幂等合并；升级后可随时保持“仅本地”模式。
- 本地学生 → 家长角色：学生主动授权（家庭账号），家长仅获得报告与限制权限，不可读取 AI 会话原文与长期记忆明细。
- 学生 → 教师：P5 起通过管理员开通，需验证教师身份（邀请码或邮箱域）。
- 教师 → 管理员：仅组织管理员手动授予，操作全部审计。

任何角色只能在其数据范围内操作；角色变化不改变既有数据归属。

## 2.2 权限原则

- 所有业务操作都在应用服务层进行资源归属校验。
- 前端路由隐藏不等于权限控制，Wails 方法必须独立校验身份和工作区。
- 导出、删除、恢复和模型密钥查看属于敏感操作，需要二次确认。
- 教师只能访问明确授权的班级数据，不能读取学生私密 AI 记忆。
- 管理员操作必须写入审计日志，敏感数据默认脱敏展示。

## 2.3 关键场景

### 首次启动

前置：应用首次安装。主流程：选择学习方向、输入考试日期和每日时长、导入可选题库、生成第一天任务。异常：日期无效、题库解析失败或模型不可用时，仍允许创建本地目标和手动任务。

### 完成一次练习

用户进入任务，系统创建练习会话，按顺序展示题目，持续保存答案草稿。提交后执行规则判分或排队 AI 评分，展示得分、解析、知识点和下一步复习动作。

### 离线复习

用户打开到期复习列表，系统只使用本地题目、历史解析和本地评分。需要 AI 时显示“联网后继续”，不得阻塞规则复习。

### 登录合并本地数据

先备份当前工作区，服务端返回远端摘要，按实体类型显示合并预览。用户确认后执行幂等合并；冲突内容保留本地副本和远端副本，绝不静默覆盖。

### 教师发布作业

后续版本中，教师选择题目版本、截止时间和评分规则，学生端拉取作业快照。作业发布后题目版本不可变，修改需生成新版本。

### 每日打卡与连续学习

用户完成当日至少一个计划任务后可打卡。系统记录连续天数（streak）、当日学习时长与完成率；漏打卡不惩罚，只重新累计。打卡状态可补签（每月限 3 次，需满足当日最低时长）。数据用于 Dashboard 展示与激励模块（见 4.11）。

### 闪卡复习

用户将错题或知识点生成闪卡，系统按简化 SM-2 排期。每日到期闪卡在 Dashboard 聚合展示，支持“不会/模糊/掌握”评级；离线可完成，评级后间隔由服务端计算。

### 专注学习（番茄钟）

用户从计划任务一键进入专注计时，可选择 25/45 分钟时长。计时期间自动隐藏非必要通知；结束后记录一段专注时段并计入学习时长统计。中断原因（休息/离开/被打断）由用户选择，用于专注度分析。

### 学习周报

每周一生成上一周学习报告：总时长、完成率、正确率趋势、薄弱知识点与下周建议。报告可导出 PDF/JSON；数据不足一周时提示“数据不足，下周生成完整报告”。

### 家长查看报告

P5 起，家长经学生授权后可查看：学习时长、完成率、正确率趋势、薄弱知识点与使用时长限制设置。家长不可查看题目答案明细、AI 会话原文与长期记忆。

### 教师批阅作业

教师查看学生作业作答，客观题自动判分，主观题可人工批阅或 AI 预批后人工确认。批阅结果即时回传学生端；申诉（见 4.4）进入教师复核队列。

## 2.4 核心对象生命周期

- 学习目标：`draft → active → paused → completed → archived`。
- 计划任务：`planned → available → in_progress → completed | skipped`。
- 练习会话：`created → answering → submitted → graded → reviewed | abandoned`。
- 题目：`draft → reviewed → published → archived`；已发布题目不可原地修改。
- AI 会话：`created → streaming → completed | failed | cancelled`。
- 学习笔记：`draft → saved → linked(题目/资料/闪卡) | detached → deleted(软删)`；笔记编辑采用自动保存，无显式发布。
- 闪卡：`active → learning → review | mastered → archived`；评级 `again/hard/good` 驱动状态迁移。
- 模拟考试：`draft → scheduled → running → auto_submitted → graded → reviewed`；考试一旦开始不允许改卷面。
- 作业（教师端）：`draft → published → in_progress(学生作答) → grading → graded → closed`；发布后题目版本冻结。
- 班级：`creating → active → archived`；学生加入需教师邀请码或审核。
- 导入批次：`preview → committing → committed | failed`；批次内题目全部校验通过才允许提交。
- 文档/资料：`uploaded → parsing → indexing → ready | failed → retrying → deleted`。
- 提醒：`scheduled → triggered → acknowledged | dismissed → rescheduled`。

所有状态变化必须检查当前状态、操作者和前置条件，并生成事件记录。

## 2.5 本章验收标准

权限测试覆盖所有核心资源；游客无法读取本地学生历史数据；重复提交不会创建重复成绩；离开未提交练习时能恢复草稿；所有非法状态迁移返回稳定错误码。新增对象（笔记/闪卡/考试/作业/班级/提醒）的状态机均有合法与非法迁移测试；家长角色只能读取授权子集且无法访问私密 AI 记忆；角色升级路径均有对应 UI 与审计记录。

---

# 第三章 系统总体架构

## 3.1 总体拓扑

```text
┌──────────────────────────────┐
│ Vue 3 + TypeScript           │
│ Router / Pinia / Renderer    │
└──────────────┬───────────────┘
               │ HTTP (POST /api/v1/{Method}) + SSE (GET /api/v1/events)
┌──────────────▼───────────────┐
│ Go Application Core          │
│ Application / Domain / Agent │
└───────┬──────────┬───────────┘
        │          │
┌───────▼──────┐ ┌─▼──────────────┐
│ SQLite       │ │ Provider Layer │
│ Files/Backup │ │ LLM/Embed/OCR  │
└──────────────┘ └──────┬──────────┘
                         │ HTTPS
                 ┌───────▼────────┐
                 │ Optional Cloud │
                 │ Sync API        │
                 └─────────────────┘
```

> **V2.0 实施基线**：传输层采用前后端分离的 Web 形态（`POST /api/v1/{Method}` 方法名式 RPC + `GET /api/v1/events` SSE 事件流 + `GET /api/v1/files` 受限下载），Go 应用服务层不依赖 HTTP；桌面端将来仅以 WebView 独立窗口渲染同一前端（或替换传输层为 Wails 绑定），业务行为与网页完全一致。

## 3.2 分层边界

- `ui`：页面、组件、状态和用户交互，不访问数据库或模型 SDK。
- `platform/wails`：只负责 DTO、绑定方法、事件转发和生命周期。
- `application`：编排用例、事务、权限和错误映射。
- `domain`：实体、值对象、状态机和不依赖基础设施的规则。
- `repository`：SQLite、文件和向量索引的抽象及实现。
- `agent`：路由、上下文、工具授权和结构化结果校验。
- `provider`：LLM、Embedding、OCR 和代码执行的可替换适配器。
- `sync`：操作日志、推送、拉取、冲突和恢复。

依赖方向只能由外层指向内层抽象。前端不得直连 SQLite，业务服务不得直接调用供应商 SDK。

### 3.2.1 组件清单（完整视图）

**前端页面（Vue）**：引导 / 今日 Dashboard / 练习（题目·答题·AI 三栏）/ 练习结果 / 计划 / 学习日历 / 错题 / 复习 / 闪卡 / 模拟考试中心 / 组卷 / 题库与资料 / 资料问答 / 笔记 / 收藏与稍后读 / 专注计时 / 报告与统计 / 成就 / 通知中心 / 设置（Provider/同步/提醒/健康/主题/语言）/ 数据管理（备份/导出/恢复）/ 教师端（班级/学生/组卷/作业/批阅/统计）/ 管理端（审核/治理/监控）/ 插件管理。

**后端服务（Go）**：workspace / user / settings / goal / plan / question / knowledge / import / practice / grading / wrong-answer / review / dashboard / document(rag) / provider-config / sync / backup / note / flashcard / exam / report / timer / achievement / reminder / favorite / calendar / group(班级) / assignment / admin / plugin / webhook / usage / health。

**跨进程契约**：全部方法名式 RPC（`POST /api/v1/{Method}`）+ SSE 事件 + 受限文件下载；详见 [API设计文档.md](API设计文档.md)。

## 3.3 传输契约（原 Wails 契约）

绑定方法统一返回：

```json
{
  "data": {},
  "error": null,
  "request_id": "01J..."
}
```

错误格式：

```json
{
  "code": "QUESTION_NOT_FOUND",
  "message": "题目不存在或已被删除",
  "retryable": false,
  "details": {}
}
```

方法命名采用 `资源 + 动词`，例如 `PracticeStart`、`PracticeSaveAnswer`、`PracticeSubmit`、`AgentChatStart`、`LibraryImport`。流式 AI 使用 `agent:delta`、`agent:tool`、`agent:completed`、`agent:error` 事件，事件都携带 `request_id`、`session_id` 和递增序号。

## 3.4 关键数据流

练习提交：前端提交答案 DTO → 应用服务校验会话 → 仓储事务保存提交 → 规则引擎判分 → 生成错题和复习任务 → 发布结果事件。AI 评分失败不回滚用户答案，只将评分状态置为 `pending` 或 `failed`。

资料问答：文件进入暂存区 → 计算哈希并解析 → 分块和嵌入 → 写入索引 → 检索授权片段 → LLM 生成带引用回答 → 保存摘要和调用元数据。

闪卡复习：错题或笔记触发生成 → 创建闪卡（评级初始为 learning）→ 到期聚合进 Dashboard → 用户评级 → 服务端按简化 SM-2 更新间隔 → 追加复习事件 → 达到 mastered 归档。

组卷考试：选择题目范围（知识点/难度/真题）→ 组卷服务按规则抽题并校验 → 生成套卷快照 → 用户开始考试（固定题目顺序与时长）→ 定时自动提交 → 复用 4.4 判分管线 → 生成成绩报告 → 错题自动进入复习队列。

统计报告：学习事件（提交/复习/专注/打卡）写入事件表 → 报告服务按日/周/月聚合 → 生成正确率趋势、时长分布、薄弱知识点 → 输出 Dashboard 卡片或可导出报告。

打卡与专注：完成计划任务或专注计时结束 → 写入 usage/checkin 事件 → 聚合 streak 与当日时长 → 更新 Dashboard 与激励状态。

作业批阅（P5）：教师发布作业快照 → 学生提交答案 → 客观题规则判分、主观题 AI 预批 → 教师确认或修改 → 评分回传 → 学生端生成成绩与错题。

题库审核（P6）：AI 生成或用户提交题目草稿 → Auditor 风险检查 → 进入审核队列 → 管理员复核 → 发布为正式题目版本 → 可被组卷与练习引用。

## 3.5 非功能要求

- SQLite 开启 WAL、外键约束和有限忙等待；写事务短小且不持有模型调用。
- 所有外部调用支持 context 取消、超时、指数退避和最大重试次数。
- 日志使用结构化字段，禁止记录 API 密钥、完整答案、完整资料和原始 Token。
- 应用启动时执行迁移、健康检查和工作区锁检查。
- 数据库备份采用一致性快照，恢复前校验版本、哈希和磁盘空间。

## 3.6 本章验收标准

架构检查确认不存在 UI 直连数据库、业务层依赖具体模型 SDK和循环依赖；断网、模型超时、迁移失败、窗口关闭取消等场景均能返回可识别错误并保持数据一致。

---

# 第四章 核心业务模块设计

## 4.1 学习目标与计划

学习目标字段包括名称、科目、考试日期、目标分数、每日学习分钟数、每周可用天数和目标知识点。Planner 根据目标、历史正确率、最近复习时间、题目难度和可用时间生成任务。

计划任务必须记录生成原因、来源目标、预计时长和优先级。用户可手动调整任务；AI 只能提出变更，不能覆盖已完成记录。

推荐排序：到期复习优先，其次是低掌握知识点、近期连续错误、目标相关性、难度匹配和题目新鲜度。每条推荐返回 `reason_codes`，例如 `REVIEW_DUE`、`LOW_MASTERY`。

## 4.2 题库与题目版本

题目统一包含：题型、题干、选项、标准答案、解析、知识点、难度、来源、标签和版本号。导入采用临时批次，逐题校验后才能发布。重复题通过内容规范化哈希和来源标识检测，导入操作使用幂等键。

题目版本不可变。编辑会创建新版本；旧版本继续被历史提交引用。AI 生成题必须标记来源、生成模型、提示词版本和审核状态。

## 4.3 练习与答题

练习会话创建时固定题目版本快照，支持暂停、恢复、跳过、返回和重新作答。答案草稿按题保存，提交时生成不可变提交记录；重新判分产生新的评分记录，不修改原始答案。

题型规则：单选必须且只能选择一个选项；多选按配置采用全选得分或部分得分；判断统一存储为单选，选项为 `A 正确 / B 错误`；填空支持空白、大小写和数值容差配置；简答进入量规评分；代码题进入隔离执行器。

## 4.4 判题与批阅

- 客观题规则引擎同步执行，结果包括得分、满分、判分规则版本和命中项。
- 简答题先执行敏感内容和长度校验，再将题目、量规和答案发送给 Grader，要求返回 JSON；低置信度结果标记为待复核。
- 代码题只接受允许的语言和文件结构，在无网络、只读文件系统、CPU/内存/进程数限制的沙箱运行，超时立即终止。
- 任何 AI 评分都展示评分依据、置信度、标准解析和申诉入口。

## 4.5 错题与间隔复习

错题记录关联题目版本、错误答案、错因、知识点和最近一次判分。错因可由规则和 Diagnoser 共同生成，分类包括概念不清、审题错误、计算错误、记忆混淆、方法不熟和表达不完整。

复习卡使用简化 SM-2 参数：`repetition`、`interval_days`、`ease_factor`、`due_at`。用户评级为“不会、模糊、掌握”，评分后更新复习间隔；复习数据只追加事件，当前卡片为投影结果。

## 4.6 模拟考试

模拟考试固定题目、顺序、时长和评分规则。考试期间自动保存，时间到期自动提交。成绩报告包含总分、分题型得分、知识点表现、耗时分布和复习建议；报告生成失败不影响成绩保存。

## 4.7 本章验收标准

覆盖空题库、重复导入、重复提交、答案恢复、时间到期、客观题边界、AI 评分失败、代码超时和错题复习到期等用例；每个状态机均有合法与非法迁移测试。

---

# 第四章补：新增业务模块详设（V2.1）

> 本章节为 4.1–4.7 的扩展。每个模块遵循统一模板：定位与目标 → 功能需求清单 → 规则与约束 → 数据模型 → 交互与页面 → 失败处理 → 验收标准。标 🔜 的模块为后续版本规划，其版本归属见第十一章。

## 4.8 学习笔记与标注 🔜（P5）

### 定位与目标

让用户在练习、资料阅读与 AI 对话过程中随手记录、沉淀与回看个人理解，形成“题目 → 笔记 → 闪卡 → 复习”的闭环；笔记与题目、资料片段、AI 消息建立可追踪关联。

### 功能需求清单

- N1. 题目笔记：在练习结果页/错题详情页对单题创建、编辑笔记；笔记自动关联题目版本、提交记录与知识点。
- N2. 资料标注：在资料阅读视图内对文本段落做高亮、划线、批注；标注内容引用资料文档、页码/段落偏移。
- N3. AI 对话收藏：可将 AI 消息（含引用）收藏为笔记草稿，保留来源会话与引用位置。
- N4. 笔记列表与全文搜索：按知识点、标签、来源（题目/资料/AI）、时间筛选；支持标题与正文关键词搜索。
- N5. 笔记转闪卡：一键将笔记转换为闪卡（正面=笔记标题或要点，背面=正文）；转换可逆（删除闪卡不影响笔记）。
- N6. 导出与同步：笔记随备份/导出（JSON/Markdown）携带；删除题目或资料时笔记保留但来源标记为“已失效”。
- N7. 自动保存与版本：编辑采用防抖自动保存（1s），保留最近 20 个自动快照，支持手动撤销/重做。
- N8. 分享（P6）：生成分享链接需先经内容安全扫描；默认不公开，可设置有效期。

### 规则与约束

- 笔记正文支持 Markdown 子集（标题、列表、代码块、行内公式）；图片以附件存储，正文存引用。
- 笔记必须归属 `workspace_id + user_id`，其他用户不可见。
- 高亮偏移基于文档稳定锚点（段落哈希），文档重新解析后锚点失效则标记“需重新定位”，不静默丢弃。
- 删除资料前提示“将失去 N 条标注的定位”，用户确认后才删除。

### 数据模型

`notes(id, workspace_id, user_id, kind[question|document|agent|free], title, body_md, source_ref{question_version_id|document_id|chunk_id|message_id}, knowledge_ids, tags, version, created_at, updated_at, deleted_at)`；`note_annotations(id, note_id, document_id, anchor_hash, offset_start, offset_end, highlight_color, note_id)`。

### 交互与页面

- 页面：笔记列表（左侧筛选栏 + 主列表 + 预览抽屉）、题目详情内嵌笔记区、资料阅读右侧标注栏。
- 全局快捷键：`Ctrl+Shift+N` 新建自由笔记；选中文本按 `Ctrl+Alt+H` 快速高亮。
- 保存状态：输入区显示“已保存 / 保存中 / 保存失败(重试)”；失败时保留本地草稿并允许重试。

### 失败处理

保存失败保留草稿并重试；锚点失效不删除正文；资料删除导致的来源失效在列表显示“已失效”徽标；同步冲突按“末写优先 + 保留旧版本快照”。

### 验收标准

创建→编辑→转闪卡→复习全链路可用；全文搜索命中率在 1 万条笔记下 P95 ≤ 200ms；高亮锚点失效可提示可重定位；删除资料不丢笔记正文；自动保存崩溃恢复不丢最近一次已确认保存。

## 4.9 闪卡与记忆强化 🔜（P5）

### 定位与目标

把错题、知识点、笔记与自定义内容转化为可排期复习的闪卡，强化长周期记忆；与间隔复习（4.5）共用简化 SM-2 参数体系，成为错题复习之外的第二种记忆通道。

### 功能需求清单

- F1. 多来源生成：从错题、题目、笔记、资料要点（Summarizer 输出）、手动创建五个来源生成闪卡。
- F2. 题型：基础问答卡（正反面）、选择题卡（正面=题目，背面=选项+解析）、填空卡（模板 `___` 挖空，可多空）、代码卡（正面=问题，背面=代码+说明）。
- F3. 每日到期队列：与错题复习合并展示或分 Tab；按到期时间与优先级排序，支持“再学一轮”。
- F4. 评级复习：`again / hard / good` 三档，服务端计算 `repetition / interval_days / ease_factor / due_at`；达到 `interval_days ≥ 21` 且连续 3 次 good 标记为 mastered。
- F5. 每日新卡限额：默认每日 20 张新卡（可调 0–200），防止新卡堆积；限额按日重置。
- F6. 批量操作：多选卡片批量归档/删除/重置进度；批量导入 CSV（正反两列）。
- F7. Anki 互通（P6）：导出 `.apkg`（正面/背面/标签/间隔），导入 Anki 导出的 CSV；导入后不自动同步评级（避免双向状态分裂），仅作为迁移工具。
- F8. 统计：今日已复习/到期总数、遗忘率（again 占比）、掌握率、月度复习日历热力图。

### 规则与约束

- 评级只允许 `again/hard/good`；间隔计算只允许在服务端执行，前端不得计算 `due_at`。
- 闪卡引用题目版本快照；题目版本更新不影响已生成闪卡的原文（闪卡正文为快照）。
- 从错题生成闪卡不改变错题/复习卡状态；删除闪卡不影响来源错题。
- 每日新卡限额与复习队列互不影响（复习无上限，仅新卡受限）。

### 数据模型

`flashcards(id, workspace_id, user_id, source[knowledge|note|document|manual], source_ref, front, back, card_type, tags, state[learning|review|mastered|archived], repetition, interval_days, ease_factor, due_at, last_reviewed_at, version, timestamps)`；`flashcard_reviews(id, flashcard_id, rating, reviewed_at, next_due_at)`（只追加事件，当前卡片为投影）。

### 交互与页面

- 页面：闪卡主页（到期队列 + 来源筛选 + 新建/导入）、复习页（单卡居中，翻面动画，三档评级按钮，快捷键 `1/2/3`）、统计页。
- 复习页支持键盘：空格翻面、`1=不会 2=模糊 3=掌握`、`Esc` 退出并保留进度。
- 未到期的卡片不出现在队列；`mastered` 卡归档后不再出现，可在归档视图恢复。

### 失败处理

评级提交失败不改变本地卡片进度（重试或跳过）；导入 CSV 部分行非法时返回行级错误清单，合法行照常导入；Anki 导出失败时保留本地 JSON 中间产物供手动迁移。

### 验收标准

五个来源均能生成卡片；评级后间隔符合简化 SM-2 公式且服务端计算；新卡限额生效；到期队列正确聚合；CSV 行级错误提示清晰；10 万卡规模下到期查询 P95 ≤ 200ms。

## 4.10 组卷与模拟考试增强 🔜（P5）

### 定位与目标

在 4.6 基础模拟考试之上，提供面向真实备考的组卷与套卷能力：手动选题、按条件自动组卷、套卷保存与复用、考试模式（防切屏/定时/顺序固定）、成绩报告与知识点分析。

### 功能需求清单

- E1. 手动组卷：从题库按知识点/难度/题型筛选后勾选题目，可调整顺序与分值，预览整卷。
- E2. 自动组卷：配置参数（知识点占比、难度分布、题量、题型构成、是否含重复题），系统按规则抽题；重复运行同一参数不保证同卷，但支持“锁定本次组卷结果”为正式套卷。
- E3. 套卷管理：套卷（exam_papers）可保存为草稿/已发布/已归档；已发布套卷题目版本冻结，修改生成新套卷版本。
- E4. 真题导入（P6）：支持按套卷格式导入真题（题组结构：大题→小题），自动识别题型与分值。
- E5. 考试模式：进入后锁定题目顺序与时长；倒计时结束自动提交；防切屏（检测窗口失焦，记录次数，超阈值标记“疑似作弊”供教师端查看）；退出考试不丢已作答。
- E6. 成绩报告：总分、分题型得分、知识点表现（掌握/薄弱）、耗时分布、难度命中分析、与历史成绩对比、复习建议。
- E7. 复盘模式：考试结束后逐题查看对错、解析、错因；支持“一键把错题加入错题本与复习队列”。
- E8. 定时考试（P6）：设定开始时间，到点自动开放，过期未考视为缺考。

### 规则与约束

- 自动组卷必须满足配置的知识点覆盖率与难度分布，否则返回 `INVALID_ARGUMENT` 并说明未满足项。
- 考试开始后不允许修改试卷；试卷题目引用题目版本快照。
- 防切屏记录属于考试事件，学生端展示“已记录 X 次离开”，教师端可见，但不自动判作弊，仅作提示。
- 同一套卷同一用户重复考试只保留最近一次成绩（历史成绩可导出）。

### 数据模型

`exam_papers(id, workspace_id, user_id, title, config_json, status, version)`；`exam_paper_sections(id, paper_id, title, order_no, question_version_ids, score)`；`exams(id, paper_id, user_id, status[running|submitted|graded|expired], started_at, ended_at, score_summary_json, suspicious_events_json)`；成绩与错题复用 `grading_results` / `wrong_answers`。

### 交互与页面

- 页面：组卷向导（4 步：范围→结构→预览→发布）、套卷列表、考试中心（开始/进行中/历史）、成绩报告页（图表+明细）、复盘页。
- 考试页隐藏所有非必要导航与快捷键（复制/粘贴受限），顶部常驻倒计时与已作答数。
- 防切屏提示采用非阻断式横幅，不强制阻断，仅记录。

### 失败处理

定时自动提交失败时以本地最后保存的答案为准并标记“自动提交中断，已用本地草稿”；成绩报告生成失败不影响成绩保存，可稍后重试生成；防切屏记录写失败不阻塞答题。

### 验收标准

手动/自动组卷均满足配置约束；套卷发布后版本冻结；考试定时自动提交与断线恢复通过故障注入；防切屏事件可追踪；成绩报告字段齐全且与判分结果一致。

## 4.11 成就、激励与习惯养成 🔜（P5）

### 定位与目标

通过打卡、连续天数、徽章、里程碑与阶段性目标，帮助用户建立可持续的学习习惯；激励规则必须透明、可解释，不使用“充值式”惩罚，不制造焦虑。

### 功能需求清单

- A1. 每日打卡：完成 ≥1 个计划任务或专注时长 ≥15 分钟即可打卡；记录连续天数与总打卡数。
- A2. 连续天数（streak）：连续打卡累计；断签后重新累计，不扣除历史数据；支持每月 3 次补签。
- A3. 徽章体系：按行为触发（首题完成、错题清零、连续复习 7 天、周目标达成、专注 100 小时、资料 100 篇等），徽章定义集中管理（可扩展）。
- A4. 里程碑：学习目标拆解为阶段里程碑（见 4.16），达成时展示庆祝动画与总结。
- A5. 周目标：用户可设定每周练习量/复习量目标，Dashboard 显示完成进度条；连续 4 周达成触发“稳定坚持”徽章。
- A6. 激励文案：低活跃时（连续 3 天未学习）在 Dashboard 显示温和提醒与“从 5 分钟开始”建议，不显示负面数据。
- A7. 隐私控制：家长模式下激励数据可见，但私密记忆不可见；所有激励数据可一键清除。

### 规则与约束

- 激励只读驱动，不得诱导付费或虚假奖励；徽章与 streak 均从事件表聚合计算，禁止手动篡改。
- 补签仅补 streak 计数，不补学习时长与正确率数据。
- 徽章触发事件去重（同一事件不重复发奖）；发放失败可重放（幂等键）。
- 所有激励文案由固定模板生成，可随语言包替换（见 4.24 国际化）。

### 数据模型

`checkins(id, user_id, date, kind[normal|makeup], minutes, created_at)`（`user_id+date` 唯一）；`user_achievements(id, user_id, achievement_id, awarded_at, event_ref, idempotency_key)`；`achievement_defs(id, code, title_key, description_key, trigger_rule_json, icon, version)`；streak 为聚合投影，不单独落表（或落 `streak_snapshots` 供报告引用）。

### 交互与页面

- 页面：成就页（徽章墙 + 已解锁/未解锁 + 解锁条件）、打卡入口（Dashboard 顶部卡片）、里程碑视图（目标进度 + 阶段点）。
- 解锁徽章时非阻断式 toast + 徽章详情弹窗；可关闭动画（设置中“减少动态效果”）。

### 失败处理

打卡与徽章发放使用幂等键，重复点击不重复记录；事件表写失败时 UI 提示稍后重试，不阻止学习主流程；时区切换以本地日期为打卡日，服务端不跨时区换算。

### 验收标准

streak 计算与断签/补签规则符合定义；徽章触发幂等；低活跃提醒在 3 天未学习时正确出现且文案温和；一键清除激励数据后徽章/打卡均消失但学习记录保留。

## 4.12 学习报告与数据洞察 🔜（P5）

### 定位与目标

把练习、复习、考试、专注与打卡数据转化为可理解、可导出、可追溯的学习报告，支撑“复盘 → 调整计划”的闭环；Dashboard（4.1 已实现）提供实时卡片，本模块提供周期性深度报告。

### 功能需求清单

- R1. 日报：当日练习量、正确率、复习完成率、专注时长、打卡状态；与近 7 日均值对比。
- R2. 周报：每周一 06:00 生成上一周报告；含总时长、任务完成率、正确率趋势、薄弱知识点 Top5、复习到期率、下周建议。
- R3. 月报：月度汇总、环比、能力画像变化（各知识点掌握度前后对比）、目标达成度、时间分配（科目/题型/时段）。
- R4. 知识点洞察：正确率、练习量、最近复习时间三维钻取；薄弱点联动“推荐练习”与“闪卡生成”。
- R5. 时间分析：学习时段分布（早/午/晚）、单次学习时长分布、专注中断原因统计。
- R6. 报告导出：PDF（模板化排版）与 JSON（原始数据）；导出包含数据来源版本与生成时间。
- R7. 对比与趋势：周与周对比、科目对比、与目标对比；数据不足时明确标注“样本不足”。
- R8. 报告订阅（P6）：可选每周邮件/推送摘要（依赖通知通道，见 4.14）。

### 规则与约束

- 报告数据全部来自事件表聚合，不允许前端传入数值；报告生成是异步任务，完成后通过事件 `report:ready` 通知。
- 样本量 < 20 次的统计不展示百分比，只显示“样本不足”。
- 报告快照存储（`reports` 表），历史报告不被新数据回写；重新生成产生新快照。
- 个人数据报告导出遵循最小化原则：不含 AI 会话原文与密钥相关字段。

### 数据模型

`reports(id, workspace_id, user_id, period[daily|weekly|monthly], period_start, period_end, payload_json, status[generating|ready|failed], generated_at)`；`report_schedules(id, user_id, kind, enabled, channel)`；聚合结果可缓存于 `report_cache(period_key, payload_json)`。

### 交互与页面

- 页面：报告中心（日报/周报/月报 Tab + 时间选择）、洞察页（图表：趋势折线、知识点雷达/柱状、时段热力图）、导出按钮。
- 报告内所有图表可点击钻取到明细列表；卡片提供“为什么这样统计”的说明入口（数据口径解释）。
- 生成中显示进度与预计时间；失败显示重试按钮。

### 失败处理

报告生成失败保留失败状态与原因，重试幂等；聚合查询超时（>10s）自动降级为简化聚合并标记“部分数据”；PDF 渲染失败回退 JSON 下载。

### 验收标准

日报/周报/月报在样本充足时字段完整且口径正确；样本不足时不展示百分比；报告快照不可变；导出 PDF/JSON 均成功且内容一致；10 万级事件聚合 P95 ≤ 5s。

## 4.13 专注计时与时间管理 🔜（P5）

### 定位与目标

提供番茄钟与自由计时两种专注模式，帮助用户进入并保持专注状态；专注数据进入报告（4.12）与打卡（4.11），与计划任务联动（从任务一键开始）。

### 功能需求清单

- T1. 番茄钟：默认 25 分钟专注 + 5 分钟休息，可配置时长（5–120 分钟）；4 轮后长休息 15 分钟。
- T2. 自由计时：自定义时长或无时限“开放式专注”，仅记录开始/结束时间。
- T3. 任务联动：从计划任务开始计时自动关联 task_id；计时完成自动标记任务进度（不自动完成任务，需用户确认）。
- T4. 中断处理：计时结束前退出需选择原因（休息/离开/被打断/误操作）；中断不计入完成轮次。
- T5. 专注统计：当日/周专注总时长、完成轮次、平均单次时长、中断率；供报告与 Dashboard 引用。
- T6. 计时通知：结束时桌面通知 + 可选提示音；专注期间隐藏非必要系统通知（应用内通知静默）。
- T7. 防打扰模式：专注期间 AI 建议、徽章动画等非阻断提示降级为静默记录，专注结束后汇总展示。

### 规则与约束

- 专注计时为本地状态机（running → paused → completed | interrupted），服务端只接收事件，不做实时心跳。
- 应用退出时未结束的计时标记为“已中断（应用退出）”，不伪造完成。
- 同一用户同一时间只允许一个活动计时；跨设备同步时以最早开始者为准（P6）。
- 番茄钟完成 = 未中断且时长 ≥ 设定时长的 90%。

### 数据模型

`timer_sessions(id, workspace_id, user_id, mode[pomodoro|free], planned_minutes, actual_seconds, task_id, status[completed|interrupted|abandoned], started_at, ended_at, interrupt_reason)`；聚合由报告服务计算。

### 交互与页面

- 页面：专注页（大计时器 + 模式切换 + 今日统计）、Dashboard 专注卡片、设置（默认时长/提示音/静默开关）。
- 计时中窗口标题显示剩余时间（桌面）；退出确认对话框只出现一次（“确认退出并记录原因”）。
- 快捷键：`Ctrl+Shift+T` 开始/暂停，`Ctrl+Shift+E` 结束。

### 失败处理

结束时网络不可用则本地暂存事件，恢复后补写（幂等）；计时事件写失败不阻塞 UI，显示“记录待同步”；应用崩溃时下次启动检测到未结束计时并询问用户如何归档。

### 验收标准

番茄钟完成判定正确；中断原因统计准确；应用退出后计时不伪造完成；专注数据进入报告与打卡；多开计时被拒绝。

## 4.14 提醒与通知中心 🔜（P5）

### 定位与目标

在合适的时间以合适的方式提醒用户复习、练习与休息；通知可配置、可静默、可追溯，不打扰不刷屏。

### 功能需求清单

- M1. 复习提醒：到期复习卡达到阈值（默认 ≥5 张）时提醒；可设每日提醒时间与星期。
- M2. 目标提醒：计划任务到期前 30 分钟提醒；考试开始前提醒（默认提前 1 小时）。
- M3. 连续学习提醒：连续 2 天未学习时温和提醒（与 4.11 低活跃提醒复用文案）。
- M4. 健康提醒：久坐提醒（默认每 45 分钟）、休息引导（番茄钟长休息）、护眼提醒（默认每 50 分钟）。
- M5. 通知中心：应用内通知列表（类型/时间/已读状态），支持全部已读、单条删除、批量清理。
- M6. 通道与免打扰：桌面通知开关、应用内通知开关、免打扰时段（默认 22:00–07:00 静默，仅保留考试提醒）；专注期间自动静默。
- M7. 提醒模板与 i18n：文案走语言包（见 4.24）；每条通知记录触发明细（哪个对象触发）。

### 规则与约束

- 通知触发由服务端调度器评估（每分钟轮询一次本地事件），前端不做轮询驱动；通知状态为投影，可重建。
- 桌面通知必须可一键关闭；免打扰时段内除考试与重要系统通知外全部延迟到时段结束。
- 通知不包含答案正文与私密内容，只含对象摘要（如“3 张复习卡到期”）。
- 用户可一键关闭某一类型通知；关闭后该类型不再触发直到重新开启。

### 数据模型

`reminders(id, workspace_id, user_id, kind[review|goal|exam|streak|health], rule_json, enabled, next_trigger_at, timestamps)`；`notifications(id, user_id, kind, title_key, body_args_json, ref_type, ref_id, read_at, created_at)`；调度状态存 `reminder_runs(id, reminder_id, triggered_at, result)`。

### 交互与页面

- 页面：通知中心（侧滑抽屉 + 独立页）、设置-提醒页（各类型开关 + 时间配置 + 测试发送）。
- “测试发送”按钮向当前设备推送一条示例通知验证通道。
- 通知点击跳转到对应对象（复习列表/计划/考试）。

### 失败处理

调度器崩溃后按 `next_trigger_at` 重新评估，过期未触发的一次性提醒标记为“错过”并补发当日摘要；桌面通知不可用时自动降级为应用内通知；免打扰与考试提醒冲突时考试提醒优先。

### 验收标准

各类型提醒按规则触发；免打扰时段正确静默；通知不泄漏正文内容；测试发送成功；调度器故障后不重复发送同一提醒（幂等）。

## 4.15 收藏、稍后读与资料增强 🔜（P5）

### 定位与目标

管理用户主动收藏的内容（题目、资料、AI 消息、笔记），提供“稍后读”队列与资料级增强能力（书签、AI 摘要、问答历史），让碎片信息可沉淀、可回看。

### 功能需求清单

- L1. 收藏：题目、资料段落、AI 消息、笔记均可收藏；收藏夹支持自定义分组（默认：全部/题目/资料/问答/笔记）。
- L2. 稍后读队列：资料条目可加入稍后读；队列按加入时间排序，可标记“已读/跳过”；支持批量清空。
- L3. 资料书签：文档内书签（锚点），快速跳转；书签随文档保留（文档删除时书签删除）。
- L4. AI 摘要：对资料文档生成摘要（要点、结构、术语表），摘要带引用定位；摘要失败可重试（见 10.9 Summarizer）。
- L5. 问答历史：资料问答（RAGAsk）历史保存在资料详情页，可回看、可导出；问答与资料同删。
- L6. 收藏搜索与导出：收藏列表支持关键词搜索；可导出为 JSON/Markdown。

### 规则与约束

- 收藏与稍后读是轻量元数据，不复制正文（引用对象 + 快照引用）；对象删除后收藏标记为“内容已删除”。
- 摘要属于派生内容：标注“AI 生成，可能存在错误”，并给出对应原文位置。
- 稍后读队列每用户上限 500 条，超出需先清空或标记已读（提示而非静默拒绝）。
- 收藏计数进入 Dashboard 与报告（可选展示）。

### 数据模型

`favorites(id, workspace_id, user_id, ref_type[question|document|agent_message|note], ref_id, group_name, note, created_at)`（`user_id+ref_type+ref_id` 唯一）；`read_later(id, workspace_id, user_id, document_id, status[queued|read|skipped], queued_at, read_at)`；`document_bookmarks(id, document_id, anchor_hash, label, offset)`；`document_summaries(id, document_id, summary_json, model, prompt_version, status, generated_at)`。

### 交互与页面

- 页面：收藏页（分组 Tab + 列表 + 搜索）、稍后读页、资料详情页（书签栏 + 摘要卡 + 问答历史）。
- 全局收藏按钮（星标）出现在题目卡、资料卡、AI 消息气泡上；收藏动效轻量。
- 摘要卡提供“重新生成”“查看原文位置”“复制”操作。

### 失败处理

摘要生成失败显示失败原因与重试；对象删除后收藏展示“内容已删除”并允许一键清理失效收藏；稍后读队列满时给出清理引导。

### 验收标准

四类对象均可收藏且去重；稍后读队列上限与状态流转正确；书签可跳转且文档删除时正确清理；摘要带引用且失败可重试；收藏搜索 P95 ≤ 200ms。

## 4.16 学习日历与目标拆解 🔜（P5）

### 定位与目标

以日历为时间主线呈现计划任务、复习、考试与打卡，支持用户把长期目标拆解为阶段里程碑与周任务，让“计划看得见、进度可感知”。

### 功能需求清单

- C1. 日历视图：月视图（默认）/周视图/日视图；展示计划任务、复习卡到期、模拟考试、打卡记录与专注时段。
- C2. 任务拖拽：周/日视图支持拖拽调整任务日期与时长；调整产生新的计划版本并记录审计。
- C3. 里程碑拆分：学习目标可拆为阶段里程碑（如“第 1 个月：完成基础 60%”），每个里程碑绑定到期日与验收条件（题量/正确率）。
- C4. 周计划模板：用户可保存常用周安排（如“周一三五数学、二四英语”）作为模板一键应用。
- C5. 复盘入口：日历上的某一天可打开当日复盘（做了什么、完成率、耗时、心情），复盘内容进入报告（4.12）。
- C6. 冲突检测：任务与考试/复习冲突时提示；自动生成任务时不与既有考试日冲突。

### 规则与约束

- 日历数据全部来自现有业务对象（任务/复习/考试/打卡）的投影，不单独维护“日历事件主数据”；仅用户手写的“个人事件”单独存储。
- 拖拽调整任务必须保留 `generated_reason` 并标记“用户手动调整”；AI 计划再次生成时以用户调整为基准，不覆盖手动调整。
- 里程碑完成判定基于其验收条件（题量达标且正确率达标），由服务端计算，用户可标记“手动完成”但需注明原因。
- 跨日移动任务需检查目标日是否已有同科目考试。

### 数据模型

`calendar_events(id, workspace_id, user_id, kind[task|review|exam|checkin|focus|personal], ref_id, event_date, start_time, duration_min, title, note)`（personal 事件可独立增删改）；里程碑并入 `learning_goals` 或独立表 `goal_milestones(id, goal_id, title, due_at, criteria_json, status, achieved_at)`。

### 交互与页面

- 页面：日历页（月/周/日切换 + 侧边“今日”速览）、目标详情（里程碑时间线）。
- 拖拽操作有确认与撤销；跨周拖拽弹确认。
- 日期格上以徽标数量提示当日任务/复习/考试数；点击进入当日明细。

### 失败处理

拖拽更新失败回滚并提示重试；冲突检测失败时禁止保存并显示冲突项；里程碑判定所需数据不足时显示“数据不足，无法判定”。

### 验收标准

月/周/日视图数据正确投影；拖拽调整生效且保留手动标记；里程碑按验收条件正确判定；冲突检测在考试日有效；个人事件与业务对象互不干扰。

## 4.17 健康与专注辅助 🔜（P5）

### 定位与目标

在长时间学习中保护用户健康：久坐提醒、用眼提醒、休息引导、夜间与护眼模式；所有提醒可配置、可关闭，不制造焦虑。

### 功能需求清单

- H1. 久坐提醒：默认每 45 分钟提醒起身活动 1–2 分钟；可关闭或调整间隔。
- H2. 护眼提醒：默认每 50 分钟提醒远眺；可与久坐提醒合并触发。
- H3. 夜间模式：按本地时间自动切换浅色/深色主题（可设“跟随系统/自定义时段”）。
- H4. 护眼色调：深色模式下可选降低蓝光（色温调节，非滤镜式全屏蒙层）。
- H5. 休息引导：番茄钟长休息时展示“喝水/拉伸/远眺”引导卡片，可跳过。
- H6. 健康统计：每日久坐次数、休息完成率，进入周报“健康”小节（可选，默认关闭）。
- H7. 无障碍联动：健康提醒遵循系统无障碍设置（见 4.24 可访问性）。

### 规则与约束

- 健康提醒为纯本地能力，不依赖网络；提醒文案走语言包。
- 所有提醒默认可一键静默；专注计时期间健康提醒保留但转为非阻断提示。
- 色温调节仅影响应用内界面渲染，不修改系统级显示设置。
- 健康统计默认不采集，用户显式开启才记录。

### 数据模型

`health_settings(workspace_id, user_id, sedentary_enabled, sedentary_interval_min, eye_enabled, eye_interval_min, night_mode[auto|light|dark|custom], night_start, night_end, blue_light_filter, stats_enabled)`；触发记录复用 `reminders` 与 `usage_events`。

### 交互与页面

- 设置-健康页：各开关与间隔配置、测试触发按钮。
- 提醒展示为应用内横幅 + 可选桌面通知；引导卡片含“已休息，继续学习”按钮。
- 主题切换即时生效并保存，重启保持。

### 失败处理

计时器由前端驱动，应用重启后重新加载剩余时间；主题切换失败回退上次主题；统计记录失败不影响提醒功能。

### 验收标准

各提醒按间隔触发且可关闭；夜间模式按设定时段切换；护眼色调可调；健康统计仅在开启时记录；专注期间提醒降级正确。

## 4.18 语音与多媒体学习 🔜（P6）

### 定位与目标

支持听力类与朗读类学习场景：题目与解析 TTS 朗读、听力题音频附件、口语评测（ASR 打分）；扩充题目与资料的媒体类型。

### 功能需求清单

- V1. TTS 朗读：题目题干/解析/笔记/闪卡正反面可朗读；语速可调（0.5–2.0x）；支持多音色配置（系统 TTS 或 Provider TTS）。
- V2. 听力题：题目类型 `listening`（音频 + 题目），音频以附件存储；支持播放速度调整与单句循环。
- V3. 口语评测：`speaking` 题型——用户录音上传，ASR 转写 + 发音/流利度/完整度打分（Provider 能力），结果进入判分管线（低置信度人工复核）。
- V4. 朗读打卡：每日朗读练习记录（可选），计入打卡时长。
- V5. 语音笔记（P7）：语音输入转文字创建笔记；离线不可用，明确提示。

### 规则与约束

- TTS 仅朗读用户可见内容，不朗读密钥/敏感字段；音频附件遵循附件存储规范（哈希/大小/路径）。
- 口语评测必须声明“AI 打分，仅供参考”；录音默认不保留原始音频（仅保留转写与评分），用户可显式开启保留。
- 听力题暂不支持自动评分（由题库自带答案或教师批阅）。
- 音频文件大小上限默认 20MB，格式 wav/mp3/m4a。

### 数据模型

`question_versions.payload_json` 扩展 `media` 字段（type[audio|speaking], attachment_path, duration_ms, transcript）; `speaking_results(id, submission_id, transcript, scores_json, audio_kept, status)`；TTS 调用记录进 `provider_calls`。

### 交互与页面

- 朗读按钮（🔊）出现在题干/解析/笔记/闪卡；音频播放器组件支持速度与循环。
- 口语题答题区为录音组件（录制/回放/重录），提交后显示评分维度明细。
- 设置-媒体页：默认语速、音色、是否保留录音。

### 失败处理

TTS 不可用时隐藏朗读按钮并提示；录音上传失败保留本地草稿可重试；ASR 失败标记评分 `failed` 并允许重试；听力附件缺失时显示“音频缺失”不阻塞其他题目。

### 验收标准

TTS 朗读覆盖四类对象且语速可调；听力题播放控制正常；口语评测评分维度齐全且低置信度进入复核；默认不保留录音且删除权生效。

## 4.19 知识图谱与能力画像可视化 🔜（P6）

### 定位与目标

把知识点关系、掌握度与学习轨迹以图谱与画像形式呈现，帮助用户直观理解“我哪里薄弱、知识如何关联”，并为推荐提供解释入口。

### 功能需求清单

- K1. 知识图谱：知识点节点 + 父子/前置关系边；节点颜色表示掌握度（绿/黄/红/灰=未学习）。
- K2. 画像雷达：按学科/知识簇展示掌握度雷达图；可切换维度（正确率/练习量/最近复习）。
- K3. 薄弱点钻取：点击薄弱节点展开：练习量、正确率、最近 10 次作答明细、关联错题与复习卡。
- K4. 学习轨迹：时间轴展示知识点从“未学习→学习中→掌握”的状态变化（来自 Profiler 事件）。
- K5. 推荐解释：练习推荐时在推荐卡片上展示“依据图谱：节点 X 薄弱”。
- K6. 图谱导出：图谱可导出 PNG/SVG（仅静态图，不含交互数据）。

### 规则与约束

- 图谱为只读可视化，数据来自 `knowledge_nodes` / `question_knowledge` / Profiler 输出，不允许前端直改。
- 掌握度计算口径统一（正确率 + 样本量加权），口径变化需版本化并在 UI 说明。
- 图谱仅渲染当前工作区授权范围；图谱规模 > 2000 节点时默认只渲染 Top-N 与薄弱子图，避免性能退化。
- 知识关系的人工维护与 AI 推断分开标注（来源字段）。

### 数据模型

`knowledge_nodes` 扩展 `relation_json` 或新增 `knowledge_relations(id, from_knowledge_id, to_knowledge_id, rel_type[parent|prerequisite|related], source[manual|ai], created_at)`；掌握度快照存 `mastery_snapshots(id, user_id, knowledge_id, mastery_score, sample_size, computed_at)`。

### 交互与页面

- 页面：知识图谱页（画布 + 缩放/平移/节点搜索）、能力画像页（雷达 + 维度切换）。
- 节点悬停显示摘要卡；点击展开详情抽屉。
- 支持“从此节点开始练习”按钮（生成针对该知识点的练习）。

### 失败处理

图谱渲染失败降级为树形列表；掌握度数据不足显示“样本不足”；图谱导出失败提示重试；关系数据损坏时回退仅显示节点。

### 验收标准

图谱节点/边与知识库一致；掌握度颜色映射正确；薄弱点钻取字段齐全；Top-N 降级策略在超大图谱下生效；导出 PNG/SVG 成功。

## 4.20 内容创作与分享 🔜（P6）

### 定位与目标

让用户与教师创作、整理、分享学习内容（题目、套卷、闪卡包、笔记），形成可复用的内容资产；分享内容必须经过安全审核且可撤回。

### 功能需求清单

- P1. 题目创作：表单式创建题目（复用 4.2 题型），AI 辅助生成题干/解析草稿（标记 AI 来源）；创作题进入 `draft`，本地可发布。
- P2. 题目请求：题库中缺失的知识点可提交“求题请求”（题目方向 + 知识点），请求进入队列（P6 管理端可见）。
- P3. 分享链接：题目/套卷/闪卡包/笔记可生成分享链接（有效期默认 7 天，可设 1/7/30 天或永久）；分享前内容安全扫描。
- P4. 导入分享：通过分享链接或文件导入他人内容；导入走标准导入管线（幂等/去重/校验）。
- P5. 内容版权声明：导入内容保留来源与作者；禁止无授权转载付费内容（声明即校验提示）。
- P6. 收藏社区（P7）：可收藏他人分享的题目到本地（副本），副本与原文版本无关。

### 规则与约束

- 分享链接必须可撤销（撤销后立即失效）；撤销操作写审计。
- 分享内容默认不包含 AI 会话上下文与个人统计，仅内容本身。
- 内容安全扫描结果缓存；扫描失败不允许发布分享。
- 用户创作题发布前需通过格式校验；AI 生成部分必须带来源标记。

### 数据模型

`shares(id, workspace_id, user_id, ref_type[question|paper|flashcard_pack|note], ref_id, token, expires_at, revoked_at, scan_result)`；`content_requests(id, workspace_id, user_id, knowledge_ids, description, status[open|fulfilled|closed], created_at)`；创作题复用 `questions` + `question_versions`（source=user_created/ai_assisted）。

### 交互与页面

- 页面：内容中心（我的创作/我导入的/分享管理）、题目创作向导、分享弹窗（有效期/复制链接/撤销）。
- 分享成功 toast + 复制链接；撤销有二次确认。
- 求题请求页：提交表单 + 我的请求列表 + 状态跟踪。

### 失败处理

安全扫描失败禁止分享并提示；链接失效展示“链接已失效”；导入分享内容失败返回行级错误；撤销后已导入副本不受影响（副本已本地化）。

### 验收标准

分享/撤销/过期全流程正确；分享前强制安全扫描；导入走幂等去重；创作题格式校验通过；求题请求状态流转完整。

## 4.21 家长/监护人模式 🔜（P6）

### 定位与目标

为 K12 场景提供家长视角：查看学习概况、设置使用限制、接收周报；严格限制数据边界，不暴露私密 AI 记忆与答案明细。

### 功能需求清单

- G1. 授权流程：学生端发起“家庭邀请”，家长凭邀请码绑定；一个学生可绑定最多 2 位家长。
- G2. 家长视图：学习时长、打卡/连续天数、任务完成率、正确率趋势、薄弱知识点（聚合级）、周报摘要。
- G3. 使用限制：每日最大学习时长提醒（默认关闭）、专注时段外提醒、可关闭应用内 AI 功能（Tutor/RAG）。
- G4. 数据边界：家长不可查看：题目答案明细、错题正文、AI 会话原文、长期记忆、笔记与闪卡内容。
- G5. 周报推送：家长可选接收周报（邮件或应用内通知通道，见 4.14）。
- G6. 解除绑定：任一方可解除；解除后家长侧数据立即失效，学生侧保留学习数据。

### 规则与约束

- 家长视图为只读聚合，禁止任何写操作（除限制设置）。
- AI 功能关闭对学生端立即生效且可在下次会话恢复；关闭不影响本地规则判分与复习。
- 授权关系与限制设置写入审计日志；解除绑定后 24 小时内彻底清理家长侧缓存数据。
- 家长模式仅限家庭自用场景，不用于学校批量监管。

### 数据模型

`family_bindings(id, student_user_id, parent_user_id, invite_code, status[pending|active|revoked], bound_at, revoked_at)`；`parent_settings(parent_user_id, student_user_id, daily_limit_min, ai_disabled, report_enabled, report_channel)`；家长视图数据由聚合服务按授权范围生成。

### 交互与页面

- 学生端：设置-家庭页（生成邀请码/管理绑定/解除）。
- 家长端：独立视图入口（应用内切换或 Web 预览），只展示聚合卡片与限制设置。

### 失败处理

邀请码过期（默认 24 小时）重新生成；绑定冲突（已达上限）明确提示；家长视图聚合失败降级为“数据暂不可用”；解除绑定立即失效且审计完整。

### 验收标准

授权/解除流程完整；家长视图聚合数据正确且不含受限字段；AI 功能开关即时生效；绑定上限与邀请码过期处理正确；解除后数据按约定清理。

## 4.22 教师端与班级协作 🔜（P5）

### 定位与目标

为教师提供班级管理、作业发布、智能组卷、批阅与学情统计能力；学生端获得作业入口与教师反馈。教师端为可选功能，不影响学生本地闭环。

### 功能需求清单

- C1. 班级管理：创建班级（名称/科目/学期）、邀请学生（邀请码/邮箱）、学生名单管理（加入/移出）、班级归档。
- C2. 学生管理：查看学生概览（学习时长、完成率、正确率、最近活跃）、学生按班级分组。
- C3. 组卷：复用 4.10 组卷能力，教师可跨班级组卷；组卷可保存为“作业模板”。
- C4. 作业发布：选择套卷/题目集 + 截止时间 + 评分规则（自动/教师批阅/混合）；发布后题目版本冻结；可更新（新版本）或撤回（未开始学生可退回）。
- C5. 批阅：客观题自动判分；主观题教师批阅或 AI 预批后确认（批量批阅、评分条）；批阅结果回传学生。
- C6. 学情统计：班级维度（完成率/平均分/正确率分布/知识点薄弱 Top）、学生维度（个人明细）、导出 CSV。
- C7. 申诉处理：学生提交的评分申诉进入教师复核队列（关联 4.4 申诉入口）。
- C8. 作业提醒：作业发布/临近截止给学生推送通知（复用 4.14）。

### 规则与约束

- 教师仅能访问被授权的班级数据；作业题目的标准答案对教师可见、对学生端在批阅完成前不可见。
- 学生私密数据（AI 记忆、笔记、闪卡、家长模式）对教师不可见。
- 作业快照引用题目版本；作业发布后修改题目需生成新版本并重新发布。
- 教师端操作全部审计；批量操作（发布/批阅）支持幂等键。
- 学生提交后到批阅完成前不能修改答案（如需修改走申诉）。

### 数据模型

`classes(id, workspace_id, owner_user_id, name, subject, semester, status, invite_code, created_at)`；`class_members(id, class_id, student_user_id, status[active|removed], joined_at)`；`assignments(id, class_id, paper_id, title, due_at, grading_rule[auto|teacher|hybrid], status[draft|published|closed], version)`；`assignment_submissions(id, assignment_id, student_user_id, submission_id, submitted_at, grade_json, graded_at)`；`grading_appeals(id, grading_id, student_user_id, reason, status[pending|accepted|rejected|resolved], teacher_note)`。

### 交互与页面

- 教师端导航：班级 / 学生 / 组卷 / 作业 / 批阅 / 统计。
- 批阅页：按作业列出待批阅学生，批量评分条 + 单题批注；AI 预批结果高亮可一键采纳/修改。
- 学生端：新增“作业”入口，显示作业列表（状态：未开始/进行中/已提交/已批阅）与成绩。
- 统计页图表：班级正确率分布、知识点薄弱 Top、完成率趋势。

### 失败处理

作业发布失败不回滚已发送通知（通知重发幂等）；批阅保存失败保留草稿；AI 预批失败回退纯人工批阅；撤回作业仅对未提交学生生效，已提交学生需教师单独处理。

### 验收标准

班级/学生/作业全流程可用；题目版本冻结生效；批阅结果正确回传；教师无法访问学生私密数据；申诉队列状态流转完整；统计口径与报告一致。

## 4.23 管理端与运营治理 🔜（P6）

### 定位与目标

为运营与平台管理员提供题库审核、内容治理、模型治理、用户治理与运营监控；管理端仅部署在服务端，不进入学生桌面端。

### 功能需求清单

- A1. 题库审核：AI 生成题/用户创作题/分享导入题的审核队列；审核操作（通过/打回/下架），打回附原因。
- A2. 内容治理：分享内容、求题请求、反馈/举报处理；违规内容下线与封禁。
- A3. 模型治理：Provider 配置白名单、模型配额与限流规则、成本看板（按 Provider/用户/日）、模型切换审批。
- A4. 用户治理：账号列表、禁用/解禁、角色管理（学生/教师/管理员）、数据导出（合规申请）。
- A5. 运营监控：DAU/激活、功能使用率、导入量/练习量/复习量趋势、错误率与崩溃率（来自 8.5 指标）。
- A6. 审计日志：管理员操作全量审计（操作者/时间/对象/变更前后）；敏感数据脱敏展示。
- A7. 公告与配置：发布应用内公告（可选）、全局功能开关（灰度）、版本发布管理。

### 规则与约束

- 管理端所有写操作必须二次确认 + 审计；批量操作限制（单次 ≤ 200 条）防误操作。
- 审核人员不能审核自己提交的内容（利益冲突规避）。
- 模型白名单外的一律拒绝；配额修改即时生效并通知受影响的教师/学生账号。
- 用户数据导出仅限合规场景，导出全程审计，导出文件默认加密。

### 数据模型

`audit_events` 扩展 `actor_role`、`before_json`、`after_json`；`review_queue_items(id, ref_type, ref_id, status[pending|approved|rejected|taken_down], reviewer_id, reason, reviewed_at)`；`content_reports(id, reporter_id, ref_type, ref_id, reason, status)`；`feature_flags(id, key, enabled, rollout_percent, updated_by)`；`provider_policies(id, provider, model, allowed, daily_quota, monthly_budget, updated_by)`。

### 交互与页面

- 管理端页面：审核工作台（队列 + 详情 + 通过/打回）、内容治理、模型治理、用户管理、监控看板、审计日志。
- 审核工作台支持键盘快捷键（`A` 通过 / `R` 打回）与批量操作。
- 监控看板图表与 8.5 指标同源。

### 失败处理

审核提交失败可重试（幂等）；批量操作部分失败返回逐条结果；配额修改冲突（版本冲突）需刷新后重试；审计写入失败时管理操作拒绝执行（审计优先）。

### 验收标准

审核/治理/治理全流程审计完整；利益冲突规避生效；模型白名单外拒绝；批量操作限制生效；审计日志不可篡改（只追加）。

## 4.24 开放能力：插件、格式矩阵与集成 🔜（P6）

### 定位与目标

以可插拔方式扩展产品能力：导入导出格式矩阵、插件系统、Webhook、命令行与国际化；保持“本地优先”前提下提供开放边界，不依赖任何单一供应商。

### 功能需求清单

- O1. 导入格式矩阵：Markdown/JSON/纯文本（✅ 已实现）、CSV、Excel（.xlsx）、Anki CSV、Word（.docx 提取文本）、PDF 文本/OCR（✅ 基础已实现）、压缩包（zip 批量导入）。
- O2. 导出格式矩阵：JSON/ZIP（✅ 已实现）、PDF（报告/套卷/错题本打印）、CSV（成绩/复习记录）、Markdown（笔记/收藏）、Anki `.apkg`（闪卡）。
- O3. 插件系统：插件 = 描述清单（manifest）+ 可执行逻辑（受限脚本或外部进程）；能力点：自定义导入解析器、自定义题型渲染、自定义 Provider 适配、自定义 Webhook 处理器、自定义报告模板。
- O4. 插件安全：插件安装需签名校验；插件运行在受限环境（无网络默认、资源限制）；插件权限声明（访问哪些数据）经用户确认；插件可随时禁用/卸载。
- O5. Webhook：可选将学习事件（练习完成/复习到期/评分完成/报告就绪）推送到用户自建端点；支持签名（HMAC）与重试。
- O6. 国际化：UI 文案全部走 i18n 资源（默认 zh-CN、en-US）；日期/数字/时区本地化；用户可切换语言（P6 起）。
- O7. 命令行（CLI）：`lumo import`、`lumo export`、`lumo backup`、`lumo doctor`（环境诊断）、`lumo migrate`（离线迁移数据目录）等命令，供高级用户与脚本使用。
- O8. 主题系统：浅色/深色/跟随系统/自定义色板；第三方主题包（P7）。

### 规则与约束

- 插件不得绕过领域服务直接写库；插件 API 面收敛为白名单（见插件 SDK 文档）。
- 导入解析器必须通过格式 fuzz 测试；非法文件不得造成崩溃或路径穿越。
- Webhook 默认关闭，启用需用户确认；事件负载最小化（不含答案正文与密钥）。
- CLI 与桌面端共享同一套应用核心（`internal/service`），不复制业务逻辑。

### 数据模型

`plugins(id, name, version, manifest_json, enabled, permissions_json, installed_at, updated_at)`；`webhook_subscriptions(id, workspace_id, url, secret_ref, event_types, enabled, created_at)`；`webhook_deliveries(id, subscription_id, event_id, status[pending|sent|failed], attempt, next_retry_at, last_error)`；i18n 资源可内嵌或存 `i18n_strings(locale, key, value, updated_at)`。

### 交互与页面

- 设置-插件页：插件列表（安装/启用/禁用/卸载/权限查看）、插件市场入口（P7）。
- 设置-集成页：Webhook 订阅管理（新建/测试发送/删除）。
- 设置-语言与外观页：语言切换、主题选择。
- CLI 通过 `lumo --help` 提供完整命令说明。

### 失败处理

插件加载失败隔离（不影响主进程）；Webhook 重试采用指数退避（最多 5 次）；导入解析器异常返回行级错误；CLI 失败返回非零退出码与可读错误。

### 验收标准

格式矩阵中每项均有导入/导出测试样本；插件安全边界测试（越权/路径/资源限制）通过；Webhook 签名校验与重试正确；i18n 覆盖全部 UI 文案；CLI 与 GUI 行为一致（同一服务层）。

## 4.25 学习体验补充需求清单 🔜（P5–P7）

> 以下为散落在各模块间的轻量能力，统一收口，避免遗漏。每个条目标注建议版本；未标注的随 P5A 一并评估。

- X1. 生词本/术语本（P5）：语言学习者可把题目/资料中的生词与术语加入生词本；与闪卡打通（一键转卡）；支持批量导入词表。
- X2. 每日一题（P5）：默认关闭的推送能力——每天固定时间推一道适配当前画像的题目；可关闭（复用 4.14 提醒）。
- X3. 错题本打印（P5）：错题按知识点/时间范围排版为可打印 PDF（含题干、答案、解析、错因）。
- X4. 快捷键总表（P5）：全局快捷键统一管理页（查看/修改/恢复默认）；新增快捷键：新建笔记、开始/暂停专注、翻卡评级、组卷、报告等。
- X5. 主题与外观（P5）：浅色/深色/跟随系统 + 强调色选择；字体大小三档（标准/大/特大，无障碍）；“减少动态效果”开关。
- X6. 数据看板自定义（P5）：Dashboard 卡片可拖拽排序/隐藏；widget 粒度：今日任务/到期复习/打卡/专注/正确率趋势/AI 建议。
- X7. 临时无痕模式（P6）：不写入长期记忆与统计的临时练习会话（如模拟体验）；退出后仅保留审计级事件（不含内容）。
- X8. 求助与分享（P6）：单题分享给朋友（链接/文本卡片）；接收方可导入为草稿题；分享内容受 4.20 审核约束。
- X9. 桌面集成（P6）：系统托盘图标、开机自启（可选）、窗口最小化到托盘、桌面通知样式统一。
- X10. 周视图计划模板（P5）：见 4.16 C4，独立入口便于复用。
- X11. 学习提醒语气（P5）：提醒文案风格可选（温和/简洁），走 i18n 语言包。
- X12. 多档案切换（P6）：同一工作区多学生档案快速切换（家庭共用电脑场景），切换不丢各自数据。
- X13. 离线知识包（P6）：可选下载/导入的本地知识包（如常用公式、词汇表），供无网络时 RAG 兜底。
- X14. 学习成就分享卡片（P7）：周报/徽章生成可分享图片卡片（含水印与数据来源说明），分享前安全扫描。

### 4.25 验收标准

X1–X14 按版本归属逐项验收：生词本与闪卡互通、每日一题可关闭、错题打印版式正确、快捷键可自定义、主题/字号即时生效、无痕模式不污染统计、桌面集成开关有效、多档案数据隔离、离线知识包检索可用。


---

# 第五章 Multi-Agent 智能体体系

## 5.1 Agent 总体原则

Agent 是受约束的能力单元，不是直接写库的自主进程。所有工具调用经过 Router、权限检查、参数 Schema 校验和审计；Agent 只能通过应用服务提交建议或结构化结果。

## 5.2 Agent 能力卡

| Agent | 触发 | 输入 | 输出 | 工具 |
|---|---|---|---|---|
| Core/Router | 用户意图或系统事件 | 消息、页面、上下文 | 意图、置信度、路由 | 无 |
| Planner | 创建或调整目标 | 目标、画像、可用时间 | 计划任务列表 | 查询题目、查询复习 |
| Profiler | 完成练习或阶段复盘 | 提交、历史事件 | 知识掌握度、证据 | 查询统计 |
| Tutor | 用户请求讲解 | 题目、答案、证据 | 引导消息、下一问 | 检索、查看解析 |
| Grader | 主观题提交 | 题目、量规、答案 | 分数、理由、置信度 | 无 |
| Diagnoser | 产生错误 | 题目、答案、历史 | 错因和纠正建议 | 查询历史 |
| Librarian | 资料问答 | 用户问题、资料权限 | 片段和引用 | 向量检索 |
| OCR | 图片/PDF 导入 | 文件 | 文本、置信度 | OCR Provider |
| Variator | 用户请求变式题 | 原题、知识点 | 草稿题目 | 查询题目 |
| Auditor | 题目入库前 | 题目草稿 | 风险、质量结果 | 无 |
| Interviewer | 模拟面试 | 目标岗位、回答 | 追问、评价 | 题库查询 |
| Coach | 学习结束或低活跃 | 画像、计划 | 反馈和行动建议 | 查询统计 |

### 5.2.1 扩展 Agent 能力卡（V2.1 规划，🔜 P5–P6）

| Agent | 触发 | 输入 | 输出 | 工具 | 版本 |
|---|---|---|---|---|---|
| Summarizer | 资料导入完成/用户请求摘要 | 文档、要点偏好 | 结构化摘要（要点/结构/术语表） | 读分块、引用定位 | P5 |
| QuizGen | 用户请求“从资料出题”或知识点扫盲 | 资料/知识点、题型偏好 | 题目草稿（复用 Auditor 审核） | 检索、读题目模板 | P5 |
| Debugger | 代码题错误/用户请求调试 | 代码、错误输出、测试用例 | 错误定位、修复建议（不直接改用户代码） | 读沙箱结果、查历史 | P5 |
| EssayGrader | 作文/论述题提交 | 题目、量规、作文 | 分维度评分、评语、示例改进 | 无（量规+结构化输出） | P5 |
| Scheduler | 目标变化/周复盘 | 目标、日历、历史完成率 | 周任务排期建议（交 Planner 合并） | 查日历、查计划 | P6 |
| Motivator | 低活跃/连续失败 | 画像、streak、失败模式 | 温和激励与行动建议（模板化） | 查询统计 | P6 |
| Interviewer+ | 面试准备增强 | 岗位、简历要点、回答 | 追问、评分维度、改进建议 | 题库查询、TTS（可选） | P6 |

> 扩展 Agent 遵循同一约束：不得直接写库、不得调用高风险工具、结构化输出必须通过 Schema 与业务校验；评估纳入 5.6 金标集。

## 5.3 Router 规则

Router 输出 `intent`、`agent`、`confidence`、`required_context` 和 `risk_level`。置信度低于阈值时展示澄清问题；涉及删除、导出、发布、代码执行等高风险操作时必须交由确定性应用服务确认。

## 5.4 工具与记忆

工具采用 JSON Schema 描述，参数禁止任意 SQL、文件路径和网络地址。短期记忆只属于当前会话；长期记忆只保存经压缩、去敏和用户允许的事实；RAG 片段必须带来源文档、页码或段落位置。

## 5.5 安全护栏

- 系统指令、用户内容、检索内容和工具结果分层传递，检索文本不能改变工具权限。
- 禁止 Agent 读取其他工作区、环境变量、密钥和未授权附件。
- 对提示注入、越权请求、危险代码和敏感信息进行输入输出检测。
- 结构化结果无法通过 Schema、业务规则或置信度校验时自动降级。

## 5.6 评估指标

建立固定金标集，至少测量路由准确率、JSON 合法率、引用正确率、Tutor 不泄露答案比例、Grader 与人工评分一致性、平均首 Token 延迟和单次成本。提示词、模型和工具版本必须进入追踪记录。

### 5.6.1 扩展评估项（V2.1）

- Summarizer：摘要要点召回率、摘要忠实度（不虚构）、引用定位准确率。
- QuizGen：生成题目的格式合法率、与知识点相关性、难度符合率；低质量题目进入 Auditor 复审比例。
- Debugger：错误定位准确率、建议采纳率、不直接改码约束合规率。
- EssayGrader：与人工评分的相关系数（≥ 0.7 目标）、分维度一致性、评语有用性抽样。
- Motivator：文案不制造焦虑合规率（模板审核）、低活跃触发准确率。
- 全部扩展 Agent 纳入统一成本与延迟预算（单次 ≤ 上限，见 10.5）。

## 5.7 本章验收标准

所有 Agent 能力卡齐全；模型无法绕过权限调用工具；结构化输出失败可恢复；固定数据集评测结果可重复；AI 会话可按 `request_id` 追踪到 Provider 调用和业务结果。扩展 Agent（Summarizer/QuizGen/Debugger/EssayGrader/Scheduler/Motivator）均有能力卡、评估口径与降级路径，未启用前不阻塞本地闭环。

---

# 第六章 SQLite 数据库设计

## 6.1 存储约定

- 主键使用 UUID 或 ULID 字符串；时间统一使用 UTC ISO 8601。
- 所有可编辑表包含 `created_at`、`updated_at`、`deleted_at`、`version`。
- 所有外键开启 `ON DELETE RESTRICT` 或明确的软删除策略。
- 数据库开启 WAL、`foreign_keys=ON`、`busy_timeout`，迁移由版本表管理。
- 原始图片、PDF 和代码文件存于应用数据目录，数据库保存相对路径、大小、MIME、哈希和加密状态。

## 6.2 核心表

| 表 | 关键字段 | 说明 |
|---|---|---|
| `workspaces` | `id`, `name`, `owner_type` | 本地数据隔离空间 |
| `users` | `id`, `workspace_id`, `display_name`, `role` | 用户档案 |
| `learning_goals` | `id`, `user_id`, `exam_at`, `target_score`, `status` | 学习目标 |
| `plan_tasks` | `id`, `goal_id`, `due_at`, `duration_min`, `status`, `reason_codes` | 计划任务 |
| `questions` | `id`, `workspace_id`, `type`, `status`, `content_hash` | 题目主实体 |
| `question_versions` | `id`, `question_id`, `version_no`, `payload_json` | 不可变题目版本 |
| `knowledge_nodes` | `id`, `name`, `parent_id` | 知识点 |
| `question_knowledge` | `question_version_id`, `knowledge_id`, `weight` | 题目知识点关联 |
| `practice_sessions` | `id`, `user_id`, `mode`, `status`, `started_at` | 练习会话 |
| `submissions` | `id`, `session_id`, `question_version_id`, `answer_json` | 原始答案 |
| `grading_results` | `id`, `submission_id`, `score`, `method`, `confidence` | 判分结果 |
| `wrong_answers` | `id`, `submission_id`, `cause`, `status` | 错题记录 |
| `review_cards` | `id`, `wrong_answer_id`, `due_at`, `interval_days` | 复习卡 |
| `agent_sessions` | `id`, `user_id`, `agent`, `status` | AI 会话 |
| `agent_messages` | `id`, `session_id`, `role`, `content_ref` | AI 消息 |
| `agent_memory` | `id`, `user_id`, `memory_type`, `summary`, `consent` | 长期记忆 |
| `documents` | `id`, `workspace_id`, `path`, `sha256`, `status` | 学习资料 |
| `document_chunks` | `id`, `document_id`, `text_ref`, `embedding_version` | 检索分块 |
| `provider_calls` | `id`, `request_id`, `provider`, `model`, `tokens`, `cost` | 模型调用审计 |
| `audit_events` | `id`, `workspace_id`, `actor_id`, `action`, `payload_json` | 操作审计 |
| `sync_operations` | `operation_id`, `entity_type`, `entity_id`, `payload`, `state` | 同步队列 |
| `schema_migrations` | `version`, `applied_at`, `checksum` | 迁移记录 |

### 6.2.1 扩展表（V2.1 规划，🔜 P5–P6）

| 表 | 关键字段 | 说明 |
|---|---|---|
| `notes` | `id`, `workspace_id`, `user_id`, `kind`, `title`, `body_md`, `source_ref`, `knowledge_ids`, `tags`, `version` | 学习笔记（4.8） |
| `note_annotations` | `id`, `note_id`, `document_id`, `anchor_hash`, `offset_start/end`, `highlight_color` | 资料高亮与批注（4.8） |
| `flashcards` | `id`, `workspace_id`, `user_id`, `source`, `source_ref`, `front`, `back`, `card_type`, `state`, `repetition`, `interval_days`, `ease_factor`, `due_at` | 闪卡（4.9） |
| `flashcard_reviews` | `id`, `flashcard_id`, `rating`, `reviewed_at`, `next_due_at` | 闪卡复习事件（只追加，4.9） |
| `exam_papers` | `id`, `workspace_id`, `user_id`, `title`, `config_json`, `status`, `version` | 套卷（4.10） |
| `exam_paper_sections` | `id`, `paper_id`, `title`, `order_no`, `question_version_ids`, `score` | 套卷题组（4.10） |
| `exams` | `id`, `paper_id`, `user_id`, `status`, `started_at`, `ended_at`, `score_summary_json`, `suspicious_events_json` | 考试实例（4.10） |
| `checkins` | `id`, `user_id`, `date`, `kind[normal|makeup]`, `minutes` | 打卡（4.11，`user_id+date` 唯一） |
| `achievement_defs` | `id`, `code`, `title_key`, `description_key`, `trigger_rule_json`, `icon`, `version` | 徽章定义（4.11） |
| `user_achievements` | `id`, `user_id`, `achievement_id`, `awarded_at`, `event_ref`, `idempotency_key` | 用户徽章（4.11） |
| `reports` | `id`, `workspace_id`, `user_id`, `period`, `period_start/end`, `payload_json`, `status` | 报告快照（4.12） |
| `report_cache` | `period_key`, `payload_json`, `computed_at` | 报告聚合缓存（4.12） |
| `timer_sessions` | `id`, `workspace_id`, `user_id`, `mode`, `planned_minutes`, `actual_seconds`, `task_id`, `status`, `interrupt_reason` | 专注计时（4.13） |
| `reminders` | `id`, `workspace_id`, `user_id`, `kind`, `rule_json`, `enabled`, `next_trigger_at` | 提醒规则（4.14） |
| `notifications` | `id`, `user_id`, `kind`, `title_key`, `body_args_json`, `ref_type/ref_id`, `read_at` | 通知（4.14） |
| `favorites` | `id`, `workspace_id`, `user_id`, `ref_type`, `ref_id`, `group_name`, `note` | 收藏（4.15，`user_id+ref_type+ref_id` 唯一） |
| `read_later` | `id`, `workspace_id`, `user_id`, `document_id`, `status` | 稍后读（4.15） |
| `document_bookmarks` | `id`, `document_id`, `anchor_hash`, `label`, `offset` | 文档书签（4.15） |
| `document_summaries` | `id`, `document_id`, `summary_json`, `model`, `prompt_version`, `status` | 资料摘要（4.15/10.9） |
| `calendar_events` | `id`, `workspace_id`, `user_id`, `kind`, `ref_id`, `event_date`, `start_time`, `duration_min`, `title`, `note` | 个人日历事件（4.16） |
| `goal_milestones` | `id`, `goal_id`, `title`, `due_at`, `criteria_json`, `status`, `achieved_at` | 目标里程碑（4.16） |
| `health_settings` | `workspace_id`, `user_id`, `sedentary_enabled`, `eye_enabled`, `night_mode`, `blue_light_filter`, `stats_enabled` | 健康辅助设置（4.17） |
| `speaking_results` | `id`, `submission_id`, `transcript`, `scores_json`, `audio_kept`, `status` | 口语评测结果（4.18） |
| `knowledge_relations` | `id`, `from_knowledge_id`, `to_knowledge_id`, `rel_type`, `source[manual|ai]` | 知识关系（4.19） |
| `mastery_snapshots` | `id`, `user_id`, `knowledge_id`, `mastery_score`, `sample_size`, `computed_at` | 掌握度快照（4.19） |
| `shares` | `id`, `workspace_id`, `user_id`, `ref_type`, `ref_id`, `token`, `expires_at`, `revoked_at`, `scan_result` | 分享链接（4.20） |
| `content_requests` | `id`, `workspace_id`, `user_id`, `knowledge_ids`, `description`, `status` | 求题请求（4.20） |
| `family_bindings` | `id`, `student_user_id`, `parent_user_id`, `invite_code`, `status`, `bound_at`, `revoked_at` | 家庭绑定（4.21） |
| `parent_settings` | `parent_user_id`, `student_user_id`, `daily_limit_min`, `ai_disabled`, `report_enabled` | 家长限制（4.21） |
| `classes` | `id`, `workspace_id`, `owner_user_id`, `name`, `subject`, `semester`, `status`, `invite_code` | 班级（4.22） |
| `class_members` | `id`, `class_id`, `student_user_id`, `status`, `joined_at` | 班级成员（4.22） |
| `assignments` | `id`, `class_id`, `paper_id`, `title`, `due_at`, `grading_rule`, `status`, `version` | 作业（4.22） |
| `assignment_submissions` | `id`, `assignment_id`, `student_user_id`, `submission_id`, `grade_json`, `graded_at` | 作业提交（4.22） |
| `grading_appeals` | `id`, `grading_id`, `student_user_id`, `reason`, `status`, `teacher_note` | 评分申诉（4.22） |
| `review_queue_items` | `id`, `ref_type`, `ref_id`, `status`, `reviewer_id`, `reason`, `reviewed_at` | 审核队列（4.23） |
| `content_reports` | `id`, `reporter_id`, `ref_type`, `ref_id`, `reason`, `status` | 内容举报（4.23） |
| `feature_flags` | `id`, `key`, `enabled`, `rollout_percent`, `updated_by` | 功能开关（4.23） |
| `provider_policies` | `id`, `provider`, `model`, `allowed`, `daily_quota`, `monthly_budget`, `updated_by` | 模型治理（4.23） |
| `plugins` | `id`, `name`, `version`, `manifest_json`, `enabled`, `permissions_json` | 插件（4.24） |
| `webhook_subscriptions` | `id`, `workspace_id`, `url`, `secret_ref`, `event_types`, `enabled` | Webhook 订阅（4.24） |
| `webhook_deliveries` | `id`, `subscription_id`, `event_id`, `status`, `attempt`, `next_retry_at`, `last_error` | Webhook 投递（4.24） |
| `usage_events` | `id`, `workspace_id`, `user_id`, `event_type`, `payload_json`, `occurred_at` | 学习/使用事件（报告与统计事实源） |

## 6.3 约束与索引

`questions(workspace_id, content_hash)` 唯一；`plan_tasks(user_id, due_at, status)`、`review_cards(user_id, due_at)`、`submissions(session_id, question_version_id)` 建索引；所有检索必须带 `workspace_id`。软删除记录不得出现在普通查询中。

### 6.3.1 扩展索引与约束（V2.1）

- `flashcards(user_id, due_at, state)`、`flashcards(user_id, source)`；`flashcard_reviews(flashcard_id, reviewed_at)`。
- `notes(user_id, kind)`、`notes(user_id, updated_at)`；`note_annotations(document_id)`。
- `exam_papers(user_id, status)`；`exams(user_id, status)`；`exams(paper_id)`。
- `checkins(user_id, date)` 唯一；`user_achievements(user_id)`。
- `reports(user_id, period, period_start)`；`report_cache(period_key)` 唯一。
- `timer_sessions(user_id, started_at)`。
- `reminders(user_id, kind, next_trigger_at)`；`notifications(user_id, read_at)`。
- `favorites(user_id, group_name)`；`read_later(user_id, status)`。
- `classes(owner_user_id)`；`class_members(class_id)`；`assignments(class_id, status)`；`assignment_submissions(assignment_id)`。
- `grading_appeals(status)`；`review_queue_items(status)`；`usage_events(user_id, event_type, occurred_at)`。
- 所有事件类表（`usage_events`、`flashcard_reviews`、`audit_events`、`sync_operations`）只追加，不 UPDATE/DELETE；业务投影表才允许更新。
- 同步对象表（笔记/闪卡/报告/收藏等）必须包含 `version` 与同步元数据字段，保证 9.3 冲突策略可实施。

## 6.4 迁移、备份与生命周期

迁移只允许向前执行，执行前创建备份并记录校验和。恢复流程为校验备份 → 停止写入 → 恢复到临时文件 → 执行兼容性检查 → 原子替换 → 重建索引。用户可导出 JSON/ZIP；删除账号或工作区时同时清理附件、索引、缓存和同步 tombstone。

## 6.5 本章验收标准

全新建库和连续版本迁移通过；外键、唯一约束、并发写入、备份恢复和异常中断测试通过；数据库中不保存明文密钥，附件路径不能跳出工作区目录。扩展表（6.2.1）在新增迁移中建立并通过：外键完整性测试、事件表只追加约束测试、扩展索引覆盖新增查询路径（闪卡到期/报告聚合/作业列表等）的性能基准、同步元数据字段可支撑 9.3 冲突策略。

---

# 第七章 前端 UI/UX 设计

## 7.1 信息架构

```text
引导
├── 今日 Dashboard
├── 开始练习
├── 计划
├── 错题复习
├── 题库与资料
├── AI Tutor
└── 设置与数据管理
```

### 7.1.1 扩展信息架构（V2.1 规划，🔜 P5–P6）

```text
学生端主导航（扩展后）
├── 今日 Dashboard（练习/复习/打卡/专注/通知聚合）
├── 练习（普通练习 / 模拟考试 / 组卷入口）
├── 计划（目标 / 计划任务 / 学习日历）
├── 复习（错题 / 闪卡 / 到期队列）
├── 题库与资料（题库 / 文档库 / 收藏与稍后读 / 笔记）
├── AI Tutor（会话 / 资料问答）
├── 报告（日报 / 周报 / 月报 / 洞察）
├── 成就（徽章 / 打卡 / 里程碑）
├── 专注（番茄钟 / 统计）
├── 设置与数据管理（Profile / Provider / 同步 / 提醒 / 健康 / 语言与主题 / 插件 / 备份恢复 / 家庭）

教师端（P5，独立入口或工作区切换）
├── 班级 / 学生 / 组卷 / 作业 / 批阅 / 学情统计

管理端（P6，仅服务端）
├── 审核工作台 / 内容治理 / 模型治理 / 用户治理 / 监控 / 审计
```

路由守卫扩展：考试页禁止路由离开（拦截确认）；专注计时页离开需确认；家长视图只读路由；教师/管理端路由按角色鉴权。

## 7.2 Dashboard

首屏展示今日任务、到期复习、连续学习天数、近期正确率、薄弱知识点和一条 AI 建议。所有指标可点击进入明细；没有数据时展示导入题库或创建目标的明确行动。

## 7.3 练习页

桌面布局为“题目区 | 答题区 | AI 助手区”，窄窗口自动改为标签页。题目区支持公式、代码和图片；答题区支持草稿自动保存、计时、跳题和提交确认；AI 区显示引用、思考引导和流式状态，不遮挡题目和答案。

## 7.4 必备状态

每个页面定义加载、空数据、离线、错误、重试、保存中、保存成功、流式生成、取消和完成状态。保存失败必须保留本地草稿；网络恢复后不自动重复提交，需显示可恢复操作。

## 7.5 设计系统与视觉语言

> **视觉主张**：Lumo AI 采用"**暖焰渐变 × 玻璃光感**"作为统一视觉语言——以 `#FF5928 → #FBA029` 橙红渐变为主色，以大面积圆角、柔和光效与玻璃拟态模糊营造温暖、专注、有活力的学习氛围，并配以细腻的非线性动效，让产品在克制的信息架构下仍具有鲜明的品牌记忆点。

- 使用统一颜色语义：主操作、成功、警告、错误、禁用和离线状态。
- 内容优先，减少装饰性卡片；重复数据使用紧凑列表和表格。
- 按钮使用图标表达撤销、重试、关闭、展开等熟悉操作，并提供可访问名称。
- 公式和代码使用独立内容区域；长文本可滚动，不能撑破布局。
- 支持键盘操作、焦点可见、语义标签和至少 WCAG AA 对比度。

### 7.5.1 视觉设计思想

- **美学主张**：暖焰渐变（主操作、强调、状态高亮）与玻璃光感（浮层、侧边栏、卡片光晕）构成双重记忆点；暖色只在需要引导注意力的地方出现，正文与次级信息保持中性灰，避免大面积暖色造成视觉疲劳。
- **拒绝通用 AI 风格**：不采用 Inter/Roboto/Arial 等默认字体观感，不使用紫色渐变配白底的俗套组合；卡片、按钮、徽章必须有明确的设计意图，而非模板化堆叠。
- **克制优先**：产品界面遵循"内容优先、utility copy"原则——组件仅在有交互或信息承载价值时使用卡片化处理；面板若可退化为普通布局而不损失语义，则不做卡片化。
- **字体选型**：展示性标题使用无衬线展示字体（如带明显字重的品牌字体），正文使用中文字体栈（`PingFang SC`/`Microsoft YaHei` 等）；等宽字体用于代码与公式区；全站字体通过 `--font-sans`/`--font-mono` token 引用，禁止局部硬编码字体。
- **对比度基线**：正文与背景对比度 ≥ 4.5:1（WCAG AA），大字号文本 ≥ 3:1；暖色主色仅用于主操作按钮（白字）与强调装饰，不用于小号正文。
- **差异化记忆点**：用户打开应用首先记住的应是"暖焰渐变的开始练习按钮 + 玻璃质感的侧边栏"这一组合，所有页面的强调行为均回归到同一渐变语言。

### 7.5.2 主题色与设计 token 规范

- 渐变主色统一定义为单一 token，全站唯一来源：

  ```css
  --gradient: linear-gradient(135deg, #FF5928, #FBA029);
  ```

- 语义色 token 命名清单（所有组件只引用 token，不直接写色值）：

  | Token | 用途 |
  |---|---|
  | `--color-primary` / `--color-primary-hover` | 主操作色（`#FF5928` 系）与悬停加深色 |
  | `--color-primary-soft` | 暖色淡底（选中背景、高亮容器） |
  | `--gradient` / `--gradient-soft` | 主渐变与其低透明度变体 |
  | `--color-success` / `-soft` | 正确、通过（保持绿系语义，不换主题色） |
  | `--color-warning` / `-soft` | 待处理、接近到期 |
  | `--color-error` / `-soft` | 错误、错题（保持红系语义） |
  | `--color-offline` / `--color-disabled` | 离线与禁用态 |
  | `--focus-ring` | 焦点环（渐变透明版本，保证键盘可达） |

- 中性色 token：`--bg`、`--bg-surface`、`--bg-sidebar`、`--text`、`--text-secondary`、`--text-muted`、`--border`、`--border-strong`，全部主题化。
- **硬规则**：任何新组件、新页面、图表配色、内联样式一律引用 token；不得出现魔法色值。主题切换后全站自动跟随，无需逐组件调整。

### 7.5.3 圆角、光效与模糊

- **圆角层级**：`--radius-sm`（10px，小元素/输入框）、`--radius`（16px，卡片/按钮/面板）、`--radius-lg`（24px，大容器/对话框）、`--radius-full`（999px，徽章/进度条/胶囊按钮）。大面积圆角是视觉语言的一部分，不是点缀。
- **光效（glow）**：主操作按钮带暖色外发光（`--glow-primary`，低透明度渐变阴影）；卡片 hover 时出现渐变描边 + 柔和光晕 + 轻微上浮（≤ 4px），强化"可交互"暗示。
- **玻璃拟态（模糊）**：侧边栏、页面头部、浮动面板与对话框使用 `backdrop-filter: blur(...)` + 半透明表面，形成玻璃光感；模糊仅用于这些高面积/浮层元素，避免滥用。
- **背景氛围**：body 使用暖色径向渐变光晕（`#FF5928`/`#FBA029` 低透明度 mesh）叠加细噪点纹理，浅色底保持 AA 对比；深色主题下光晕降为低亮度暖光。
- **性能边界**：同一视口内 `backdrop-filter` 元素不超过 3 个；动画属性优先使用 `transform`/`opacity`（GPU 合成），避免大面积 `filter`/`box-shadow` 动画。

### 7.5.4 动效规范

- **统一缓动曲线**：全局非线性动效曲线 `cubic-bezier(0.22, 1, 0.36, 1)`（ease-out-quint 风格），禁止默认线性 transition 造成生硬观感；所有 `transition`/`animation` 引用统一 token（`--ease-out`、`--ease-spring`）。
- **时长上限**：微交互 120–200ms，页面级动效 250–400ms；不允许超过 500ms 的常驻动画。
- **页面入场**：主要页面采用交错 reveal（子元素 `animation-delay` 递增 40–80ms 阶梯入场），每次进入页面只做一次，不重复打扰。
- **hover 微交互**：按钮按压缩放（`scale(0.98)`）、图标 hover 微动、卡片上浮光晕、进度条渐变流动——均为低幅度、高完成度的细腻动效。
- **降级开关**：检测到 `prefers-reduced-motion` 时关闭全部非必要动效（入场、光晕、流动），仅保留状态变化的瞬时反馈；此降级必须覆盖所有页面。

### 7.5.5 深浅双主题

- **双套 token**：light 与 dark 各维护一套完整语义色 + 中性色 token（CSS 变量 + `color-scheme` 声明）；两主题下语义色均达到 WCAG AA 对比度。
- **切换方式**：默认跟随系统偏好（`prefers-color-scheme`），用户在设置中可手动选择浅色/深色/跟随系统；选择结果由 `theme` store 持久化（衔接 7.6.1），重启后保持。
- **深色主题要求**：深色模式下禁用纯黑背景（避免刺眼），使用深暖灰 + 低亮度暖光晕；玻璃拟态在深色下降低透明度并加深模糊，保证浮层与背景可区分。
- **无漏配检查**：主题切换后全页面（含图表、徽章、错误态、滚动条、焦点环）不得出现任一主题下的低对比或硬编码色块；验收时逐页走查两主题。

### 7.5.6 响应式与自适应

- **断点**：桌面 ≥ 1024px、平板 768–1023px、移动 ≤ 767px；布局以桌面为基准，向小屏逐级降级。
- **侧边栏折叠**：平板与移动端侧边栏由 220px 全宽折叠为 64px 图标栏（保留品牌图标与可访问名称）；移动端进一步收起为抽屉式导航（汉堡按钮展开，遮罩可关闭）。
- **练习页三栏 → 标签页**：桌面为"题目区 | 答题区 | AI 助手区"三栏；窄窗口自动改为标签页（衔接 7.3），标签可切换且不丢当前作答。
- **触控目标**：所有可点击元素最小 44×44px（移动端）；桌面端 hover 态在触屏上自动退化为点击态。
- **无横向溢出**：窄窗口下表格横向滚动、代码区独立滚动、公式自适应缩放，页面整体无横向溢出（验收见 7.7）。
- **缩放友好**：`viewport` 元数据正确；字号使用 `rem` 相对单位，允许系统字体缩放无障碍放大。

### 7.5.7 硬编码颜色禁令与视觉统一

- **禁止硬编码**：任何颜色值（含组件局部样式、内联样式、图表序列色、第三方组件覆盖样式）必须来自设计 token；SVG 图标色同样走 token（当前色 `currentColor` 优先）。
- **提交前校验**：样式改动提交前执行 grep 校验——除 token 定义文件外，不允许出现十六进制色值、`rgb()/hsl()` 字面量与"蓝色系遗留色值"（如 `#2563eb`）残留；CI 可将此作为检查项。
- **视觉统一**：新增页面必须复用既有 `.card`/`.btn`/`.stat-card`/`.badge`/`.option-item`/`.progress` 等全局组件类与 token，禁止为单个页面另起一套配色或组件风格。
- **一致性评审**：每个里程碑的 UI 评审包含"视觉统一性"检查项——渐变方向、圆角半径、光效强度、模糊程度在全部页面保持一致；主题 token 变更必须走全站回归。

## 7.6 前端状态与路由

Pinia 按 `session`、`practice`、`library`、`agent`、`settings` 拆分。服务器或本地数据进入 store 前先校验 DTO。导航守卫检查未保存答案和未完成导入；所有异步请求支持取消和请求序号，避免过期响应覆盖新状态。

### 7.6.1 扩展 store 与状态（V2.1）

- 新增 store：`notes`、`flashcards`、`exam`（组卷与考试）、`report`、`timer`（专注）、`achievement`（打卡/徽章）、`notification`、`favorite`（收藏/稍后读）、`calendar`、`family`、`teacher`（班级/作业/批阅）、`admin`（审核/治理）、`plugin`、`i18n`、`theme`。
- 所有新 store 遵循同一约定：进入前 DTO 校验、写操作走幂等键、离开页面保存草稿、`version` 参与更新请求。
- 全局共享状态：`workspace` 与 `user` 保持单一来源；角色（student/teacher/admin/parent）驱动路由守卫与视图裁剪。
- 主题与 i18n 状态全局持久化到本地设置；语言切换即时生效且不丢页面状态。
- 考试计时与专注计时状态在应用重启后从服务端事件恢复（不依赖前端内存）。

## 7.7 本章验收标准

核心任务可完全用键盘完成；窄窗口不出现横向溢出；练习草稿在重启后恢复；AI 流式过程中可取消；自动无障碍扫描无关键问题，常用分辨率完成视觉回归。扩展页面（闪卡/报告/日历/专注/成就/通知/教师端/管理端）均通过：加载/空/错误/离线状态走查、键盘全流程、窄窗口布局回归、i18n 切换无文案缺失、角色鉴权路由测试。

**UI 风格专项验收（对应 7.5）：**

- **主题切换验收**：浅色/深色/跟随系统三种模式切换后全页面走查——语义色、中性色、图表、徽章、滚动条、焦点环均正确跟随 token，无任一主题下的低对比或漏配色块；`theme` store 持久化生效，重启后保持上次选择。
- **硬编码颜色验收**：提交前 grep 校验通过——除 token 定义文件外，全库无十六进制色值、`rgb()/hsl()` 字面量与蓝色系遗留色（`#2563eb`）残留；新组件/页面/图表配色全部走 token。
- **视觉语言验收**：`#FF5928 → #FBA029` 渐变在主操作（开始练习、提交等）可见且方向统一；圆角、glow 光效、`backdrop-filter` 玻璃模糊在按钮/卡片/侧边栏/进度条/聊天气泡上可见且强度一致。
- **动效验收**：全局非线性缓动曲线生效，无默认线性 transition 残留；页面入场交错 reveal 仅一次；`prefers-reduced-motion` 下全部非必要动效关闭，仅保留状态瞬时反馈。
- **响应式验收**：桌面/平板/移动三档断点布局正确——侧边栏 220px → 64px 图标栏 → 抽屉式导航逐级降级；练习页三栏 → 标签页切换不丢作答；触控目标 ≥ 44px；窄窗口无横向溢出。
- **视觉回归**：对主要页面（Dashboard/练习/复习/计划/Tutor/设置）执行两主题 × 三断点的视觉回归，确认渐变、圆角、光效、模糊与动效统一无回归。

---

# 第八章 Go 后端工程设计

## 8.1 工程目录

```text
lumo/
├── cmd/app
├── internal/app
├── internal/domain
├── internal/service
├── internal/repository
├── internal/agent
├── internal/provider
├── internal/platform/wails
├── internal/sync
├── internal/database/migrations
└── frontend
```

## 8.2 包职责

`domain` 只包含实体和规则；`service` 实现用例和事务；`repository` 定义并实现持久化；`provider` 隔离外部服务；`agent` 编排 AI；`platform/wails` 负责桌面适配；`sync` 只处理变更传输，不承载领域判分。

### 8.2.1 扩展服务与包（V2.1 规划，🔜 P5–P6）

- 新增 service 文件：`note.go`、`flashcard.go`、`exam.go`（组卷/考试）、`report.go`、`timer.go`（专注）、`achievement.go`（打卡/徽章/streak）、`reminder.go`（提醒调度）、`notification.go`、`favorite.go`、`calendar.go`、`family.go`、`health.go`、`speaking.go`、`knowledge_graph.go`、`share.go`、`content_request.go`、`group.go`（班级）、`assignment.go`、`appeal.go`、`admin.go`、`plugin.go`、`webhook.go`、`usage.go`（事件埋点）、`i18n.go`。
- 新增 repository：与上述 service 一一对应的仓储接口与 SQLite 实现。
- 新增 agent 能力：`summarizer.go`、`quizgen.go`、`debugger.go`、`essaygrader.go`、`scheduler.go`、`motivator.go`（复用 5.2.1 能力卡）。
- 新增 provider：`tts`、`asr`（口语评测）、`pdf`（增强解析）适配器；所有新 Provider 遵循 10.1 契约。
- 新增 platform 目录：`platform/desktop`（当前 Web 传输层）与规划中的 `platform/wails` 共存；`platform/cli`（4.24 CLI 命令）。

## 8.3 配置与依赖注入

配置来源优先级为默认值、配置文件、环境变量、用户设置。密钥存操作系统凭据库，配置文件只保存 Provider 名称和非敏感参数。应用启动时构建 Repository、Provider、Service 和 Agent Router，测试使用内存或 fake 实现。扩展：插件与 Webhook 通过独立注册表注入，不侵入核心 DI 图；功能开关（feature_flags）在启动与运行期均可读取。

## 8.4 错误与事务

领域错误包括资源不存在、状态非法、权限不足、校验失败、冲突和外部依赖失败。应用层将其映射为稳定错误码。数据库事务不得跨越模型调用；模型调用结果先校验，再在短事务内写入。

## 8.5 可观测性

每个请求携带 `request_id` 和 `workspace_id`；日志记录事件名、耗时、结果和错误码，不记录秘密与完整用户内容。指标包括启动耗时、SQLite 锁等待、练习提交耗时、Provider 延迟、Token、成本、错误率和崩溃数。

## 8.6 测试矩阵

- 单元测试：领域规则、状态机、评分、复习算法和 DTO 校验。
- 仓储集成测试：迁移、事务、索引、并发和备份恢复。
- Provider 契约测试：超时、重试、取消、限流和结构化输出。
- Wails 绑定测试：参数映射、事件序列和错误码。
- 端到端测试：引导、练习、错题、导入、AI 降级和数据导出。

### 8.6.1 扩展测试矩阵（V2.1）

- 新增模块单元测试：闪卡 SM-2 计算、组卷约束满足、报告聚合口径、打卡/徽章幂等、提醒调度、家长数据边界、插件权限校验、Webhook 签名与重试。
- 新增集成测试：扩展表迁移与索引、事件表只追加约束、10 万级报告聚合性能、作业/班级数据隔离。
- 新增契约测试：TTS/ASR Provider 超时与降级、Summarizer/QuizGen 结构化输出合法率。
- 新增安全测试：插件越权/路径穿越/资源限制、分享链接撤销与过期、管理端审计不可篡改、家长越权读取（AI 记忆/答案明细）拒绝。
- 新增故障注入：考试断线自动提交、提醒调度器崩溃恢复、Webhook 投递失败重试、报告生成中断恢复。

## 8.7 本章验收标准

干净环境可构建；静态检查、单元和集成测试通过；架构测试禁止 UI 或业务层直接引用具体 SDK；Provider 故障能映射到前端可恢复状态。扩展服务按 8.2.1 落地后，新增模块测试全绿、插件/Webhook 安全边界测试通过、故障注入场景不丢数据且错误可恢复。

---

# 第九章 云端与本地同步设计

## 9.1 同步原则

本地 SQLite 是事实源。MVP 提供加密导出/导入和可选云备份；多设备同步在独立里程碑实现。同步不上传整个数据库文件，而是上传版本化、幂等的变更操作。

## 9.2 操作格式

```json
{
  "operation_id": "01J...",
  "device_id": "device-uuid",
  "workspace_id": "workspace-uuid",
  "entity_type": "review_card",
  "entity_id": "entity-uuid",
  "base_version": 3,
  "operation": "update",
  "payload": {},
  "created_at": "2026-08-04T00:00:00Z"
}
```

服务器以 `operation_id` 幂等；客户端按本地队列顺序推送，成功后记录服务端序列号，再拉取游标之后的变更。

## 9.3 冲突策略

- 计划标题、标签等低风险字段：字段级合并。
- 用户偏好：末写优先，但保留审计记录。
- 题目版本和标准答案：不可合并，生成冲突副本。
- 答案、评分、学习事件：只追加，禁止覆盖历史事实。
- 删除：使用 tombstone，恢复操作产生新版本。

### 9.3.1 扩展对象同步分类（V2.1 规划）

| 对象 | 同步策略 | 冲突处理 |
|---|---|---|
| 学习笔记（4.8） | 整文档版本化同步 | 末写优先 + 保留旧版本快照（可在本地回滚） |
| 闪卡（4.9） | 字段级（评级与间隔服务端计算） | 评级事件只追加；间隔以服务端重算为准 |
| 套卷/考试（4.10） | 套卷版本化；考试事件只追加 | 套卷冲突生成副本；考试记录不合并 |
| 打卡/徽章（4.11） | 事件只追加 | 幂等去重（user_id+date 唯一） |
| 报告快照（4.12） | 只读派生数据，不参与冲突 | 以生成时间为准 |
| 专注计时（4.13） | 事件只追加 | 同用户同时间多开时以最早开始为准 |
| 提醒/通知（4.14） | 规则字段级；通知只追加 | 规则末写优先；通知幂等去重 |
| 收藏/稍后读（4.15） | 字段级 | 末写优先，保留审计 |
| 日历个人事件（4.16） | 字段级 | 末写优先 + 版本号 |
| 健康设置（4.17） | 字段级 | 末写优先 |
| 家庭绑定（4.21） | 状态机事件 | 撤销以服务端时间为准 |
| 班级/作业（4.22） | 服务端权威，客户端只读拉取 | 教师端为事实源，不双端编辑 |
| 审核/治理（4.23） | 服务端权威 | 不参与客户端同步 |
| 插件/Webhook（4.24） | 服务端或本机配置 | 本机配置按工作区隔离 |

原则：凡可编辑且跨设备共享的对象必须有 `version` 与冲突策略；凡事件型数据只追加并幂等；教师/管理端对象以服务端为权威源。

## 9.4 安全与恢复

使用短期访问令牌、刷新令牌和设备撤销；传输使用 HTTPS，敏感备份在本地先加密。首次同步展示摘要和冲突数量；账号切换前自动备份；同步失败保留队列并采用退避重试。用户可下载诊断包，诊断包默认脱敏。

## 9.5 云端接口

核心接口为 `POST /v1/devices`、`POST /v1/sync/push`、`GET /v1/sync/pull?cursor=`、`POST /v1/backups` 和 `DELETE /v1/workspaces/{id}`。接口必须支持幂等键、分页、游标、错误码和服务端时间。

## 9.6 本章验收标准

双设备离线编辑、重复投递、网络中断、时钟漂移、删除后恢复和服务端返回 409 均通过；任何同步故障都不丢失本地数据，并能显示待处理数量和恢复动作。扩展对象（笔记/闪卡/套卷/打卡/收藏/日历/家庭绑定等）按 9.3.1 分类通过同步测试：幂等去重、冲突策略、只追加约束与服务端权威对象（班级/作业/审核）不可被客户端篡改。

---

# 第十章 AI 技术实现方案

## 10.1 Provider 抽象

定义 `LLMProvider`、`EmbeddingProvider`、`OCRProvider` 和 `CodeRunner` 接口。接口包含模型名、超时、取消、流式回调、结构化 Schema、Token 用量、费用估算和错误分类。业务层只依赖接口，不依赖供应商类型。

## 10.2 RAG 管线

```text
文件校验 → 哈希去重 → 文本解析/OCR → 清洗
→ 按标题与语义分块 → Embedding → 索引
→ 权限过滤 → 混合检索 → 重排 → 引用拼装 → LLM 回答
```

支持 Markdown、TXT、PDF 和图片。分块保留文档、章节、页码、段落和偏移元数据；Embedding 版本变化时建立新索引，完成后原子切换。检索默认结合关键词和向量，Top-K 结果经过权限过滤和去重。

## 10.3 回答约束

Tutor 需要区分题目事实、资料引用和推理建议。无足够证据时明确说明不确定性，不编造来源。引用展示文档名称、位置和匹配片段。涉及标准答案时优先使用题目版本中的人工或审核内容。

## 10.4 记忆系统

短期记忆保存当前题目、用户回答和最近对话；长期记忆只保存“常错知识点、偏好讲解方式、学习时间习惯”等摘要。记忆写入需满足去敏、去重、用户授权和可删除条件；原始长对话不默认永久保存。

## 10.5 可靠性与成本治理

单次调用设置连接超时、总超时、最大输出 Token、重试次数和熔断阈值。按用户和工作区设置每日配额；超额后切换小模型、使用缓存或回退模板。缓存键包含模型、提示词版本、输入哈希和资料版本，避免旧答案污染新题目。

## 10.6 隐私与安全

敏感内容分类为身份信息、学习内容、凭据和高风险代码。发送前按用户设置脱敏；日志只保存哈希、长度、Token 和错误信息。支持关闭云模型、使用本地模型、删除资料索引和删除长期记忆。模型服务商的留存政策必须在设置页明确展示。

## 10.7 AI 评测

评测集覆盖公式、长资料、OCR 噪声、相似题、错误答案、提示注入和无证据问题。指标包括 Recall@K、MRR、引用准确率、答案忠实度、Tutor 引导合规率、评分一致性、P95 延迟和平均成本。每次模型或提示词变更必须重新评测并记录版本。

## 10.8 本章验收标准

资料问答可定位引用；无证据问题不会伪造答案；Provider 超时可取消并重试；敏感信息不进入日志；固定评测集结果可重复且低于阈值时阻止发布。

## 10.9 资料摘要与要点提取（Summarizer）🔜（P5）

- 触发：资料导入完成（自动，可关）或用户显式请求。
- 输入：文档分块（授权范围内）、用户要点偏好（可选）。
- 输出（JSON Schema）：`{title, key_points[], structure[], glossary[]{term, definition}, citation_refs[]}`；每条要点必须带 `chunk_id` 定位。
- 约束：摘要长度上限（默认 ≤ 800 字）；无证据要点丢弃；忠实度评测进入 5.6.1。
- 降级：模型不可用时不生成摘要，资料问答不受影响；摘要状态 `failed` 可重试。
- 存储：`document_summaries`（含 model、prompt_version），重新生成产生新记录。

## 10.10 资料出题与知识点扫盲（QuizGen）🔜（P5）

- 触发：用户请求“从资料出题”“扫盲某知识点”。
- 输入：资料/知识点、题型偏好（默认客观题）、题量（1–10）。
- 输出：题目草稿列表（题干/选项/答案/解析/知识点），全部进入 `draft` 状态并经 Auditor 检查。
- 约束：生成题不得自动发布；与已有题去重（内容哈希）；答案不得与资料原文冲突（冲突标记人工复核）。
- 降级：无模型时回退为“基于已有题目的筛选推荐”，不生成新题。

## 10.11 口语评测与语音朗读（ASR/TTS）🔜（P6）

- ASR：录音上传 → 转写 → 评分（发音/流利度/完整度/语法）；分数结构化写入 `speaking_results`。
- 约束：默认不保留原始录音（用户可开启保留）；评分声明“AI 打分，仅供参考”；低置信度进入人工复核。
- TTS：朗读题干/解析/笔记/闪卡；语速 0.5–2.0x；Provider 抽象（系统 TTS / 云 TTS），不可用时隐藏朗读入口。
- 评测：ASR 转写准确率、评分与人工一致性抽样；TTS 延迟预算（首句 ≤ 1.5s）。

## 10.12 代码调试助手与作文批改（Debugger/EssayGrader）🔜（P5）

- Debugger：输入代码 + 错误输出/测试用例（来自沙箱），输出错误定位、原因、修复建议与示例（不直接改用户代码）；仅允许读取沙箱结果，无写权限。
- EssayGrader：量规 + 作文 → 分维度（内容/结构/语言/规范）评分、总评与改进建议；与人工评分相关系数 ≥ 0.7 为发布门槛。
- 两者均复用 4.4 的“低置信度人工复核”模式与申诉入口。

## 10.13 本地模型与多模态 🔜（P6）

- 本地模型：抽象出 `LocalProvider`（Ollama / LM Studio / llama.cpp 兼容接口）；本地模型可完全替代云模型（LLM/Embedding），密钥零配置。
- 多模态：图片题目理解（图表/截图 OCR+理解）、手写识别（草稿拍照转文字）；沿用 10.2 管线，增加 `vision` 步骤。
- 约束：本地模型不满足延迟/质量门槛时提示并回退云模型或模板；多模态输入先压缩与脱敏。

## 10.14 变式题生成与模拟面试增强 🔜（P6）

- Variator 增强：基于原题 + 知识点生成变式草稿（改数字/换情境/换题型），全部经 Auditor；变式不改变知识点标签。
- Interviewer+：岗位画像 + 行为问题 + 追问；输出评分维度与改进建议；支持 TTS 朗读题目（可选）。
- 两者均写入 `provider_calls` 并纳入成本预算；生成结果可一键导入题库草稿。

## 10.15 扩展 AI 能力验收标准（V2.1）

Summarizer 摘要忠实度与引用准确率达标；QuizGen 生成题全部经审核且与题库去重；ASR/TTS 延迟与质量达标且默认不保留录音；Debugger/EssayGrader 不越权写库且评分一致性达标；本地模型可零密钥切换；变式题/面试输出可追溯且成本在预算内。

---

# 第十一章 开发路线规划

> **V2.0 实施状态（2026-08）**：P0（基础骨架）、P1（本地练习闭环）、P2（AI 辅导）、P3（资料导入与 RAG）、P4（备份与同步骨架）均已按本路线完成实现并通过端到端冒烟（`scripts/smoke.ps1` 15 项全过）；P5（教师端与跨平台发布）为后续里程碑。同步当前为本地模拟实现（sim-server 文件存储），云端真实部署沿用既有操作协议。

## 11.1 P0 基础骨架

交付：Wails 桌面壳、Vue 路由、Go 分层、SQLite 迁移、工作区、配置和错误模型。退出标准：干净环境构建成功，创建工作区并完成备份恢复。

## 11.2 P1 本地练习闭环

交付：题库导入、题目版本、练习会话、草稿、客观题判分、错题和复习卡。退出标准：核心端到端测试通过，断网可完整练习，重复提交和异常退出不丢数据。

## 11.3 P2 AI 辅导与能力画像

交付：Router、Planner、Profiler、Tutor、Grader、Diagnoser、Provider 抽象和成本记录。退出标准：结构化输出校验、取消、重试、安全测试和金标评测通过。

## 11.4 P3 资料导入与 RAG

交付：PDF/OCR、分块、Embedding、混合检索、引用和索引管理。退出标准：评测集达到召回、引用和延迟阈值，资料权限隔离有效。

## 11.5 P4 备份与云同步

交付：加密导出、备份、设备注册、操作日志、拉推、冲突副本和恢复界面。退出标准：双设备故障注入测试通过，无本地数据丢失。

## 11.6 P5 教师端与跨平台发布

交付：班级、作业、智能组卷、AI 批阅、Windows/macOS/Linux 安装包。退出标准：权限隔离、签名、自动更新、回滚和平台兼容性测试通过。

## 11.7 质量闸门

- 功能：P0 缺陷为零，核心流程有自动化测试。
- 数据：迁移、备份、恢复和删除流程通过演练。
- 安全：密钥、路径、代码执行、提示注入和越权测试通过。
- 性能：启动、页面响应、数据库查询和 AI 延迟达到目标。
- 体验：核心任务可用性测试通过，键盘与无障碍检查无关键问题。
- 发布：版本、迁移、依赖、许可证、隐私政策和回滚包齐全。

## 11.8 主要风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 模型回答不稳定 | 错误学习反馈 | 结构化输出、引用、评测和人工复核 |
| Provider 限流或涨价 | AI 不可用 | 配额、缓存、多 Provider、模板降级 |
| 代码执行逃逸 | 严重安全事件 | 独立沙箱、无网络、资源限制和审计 |
| 数据迁移失败 | 用户数据损失 | 自动备份、临时恢复、校验和和回滚 |
| Wails 平台差异 | 发布延期 | 尽早构建三平台 CI 和安装测试 |

## 11.9 后续路线详案（V2.1 规划）

### 11.9.1 P5A 学习体验增强（学生端）

交付：学习笔记与标注（4.8）、闪卡（4.9）、组卷与模拟考试增强（4.10）、打卡与成就（4.11）、报告中心（4.12）、专注计时（4.13）、提醒通知（4.14）、收藏与稍后读（4.15）、学习日历（4.16）、健康辅助（4.17）、扩展 Agent（Summarizer/QuizGen/Debugger/EssayGrader，5.2.1）。退出标准：全部新增模块验收标准通过；扩展测试矩阵（8.6.1）绿色；10 万级数据下性能达标；i18n 骨架可用（zh-CN/en-US）。

### 11.9.2 P5B 教师端与跨平台发布

交付：班级/学生/作业/组卷/批阅/学情统计（4.22）、评分申诉、桌面三平台安装包（签名/自动更新/回滚）。退出标准：权限隔离与审计测试通过；平台兼容矩阵通过；P5A 与 P5B 无功能回归。

### 11.9.3 P6 管理端、开放能力与语音

交付：管理端（审核/治理/模型治理/监控，4.23）、家长模式（4.21）、知识图谱（4.19）、内容创作与分享（4.20）、插件系统与 Webhook（4.24 基础）、语音与多媒体（4.18，ASR/TTS）、本地模型支持（10.13）、真实云端同步（第九章）。退出标准：管理端审计不可篡改；插件安全边界测试通过；语音与本地模型延迟/质量达标；真实云端同步故障注入通过。

### 11.9.4 P7 协作、社区与开放生态

交付：题库市场/内容社区（4.20 增强）、求题请求闭环、移动端 Web 预览、第三方主题与插件市场、Webhook 扩展。退出标准：社区内容治理与举报闭环可用；移动端核心任务可用；插件市场审核流程可用。

### 11.9.5 P8 多端与长期演进

交付：iOS/Android 移动端、桌面-移动协同、更多 AI 模态（视频讲解、白板）、组织版（学校/机构批量部署）。退出标准：多端数据一致性与冲突策略（9.3.1）全场景通过；组织版权限与合规评审通过。

## 11.10 需求优先级矩阵（V2.1）

> 优先级：P0=已实现核心、P1=已实现闭环、P2–P4=已实现增强、P5A/B–P8=规划。MoSCoW：Must（必须）、Should（应当）、Could（可以）、Won't（本期不做）。

| 需求 | 优先级 | 依赖 | 风险 |
|---|---|---|---|
| 笔记/闪卡/报告/专注/提醒（4.8–4.15） | P5A · Must | 4.x 领域规则、6.2.1 表、8.2.1 服务 | 数据量大时聚合性能 |
| 组卷与考试增强（4.10） | P5A · Must | 4.6 考试管线、题目版本 | 防切屏误判 |
| 打卡/成就/日历（4.11/4.16） | P5A · Should | 事件表 usage_events | 时区与跨日 |
| 健康辅助（4.17） | P5A · Should | 提醒调度（4.14） | 打扰用户 |
| 教师端（4.22） | P5B · Must | 服务端账号体系 | 权限边界 |
| 管理端（4.23） | P6 · Must | 教师端、审计 | 审计不可篡改 |
| 插件/Webhook（4.24） | P6 · Should | 服务层收敛 API | 插件安全 |
| 语音/本地模型（4.18/10.13） | P6 · Should | Provider 抽象 | 质量与延迟 |
| 家长模式（4.21） | P6 · Should | 角色模型 | 隐私边界 |
| 知识图谱/创作分享（4.19/4.20） | P6 · Could | Profiler、审核队列 | 图谱规模 |
| 社区/移动端（P7/P8） | P7–P8 · Could | 开放能力 | 治理与合规 |
| 实时课堂/社交/公开市场 | — · Won't（本期） | — | 范围蔓延 |

## 11.11 补充风险与依赖（V2.1）

| 风险 | 影响 | 缓解 |
|---|---|---|
| 新增模块挤压本地闭环稳定性 | 回归 | 每个里程碑先跑 P0–P4 端到端冒烟 |
| 插件/Webhook 引入安全面 | 数据泄露 | 权限声明、签名校验、受限执行、审计 |
| 语音/多模态成本高 | 预算超支 | 配额、本地模型优先、明确计费展示 |
| i18n 文案漂移 | 体验下降 | 文案走语言包、缺失键 CI 拦截 |
| 教师/管理端与本地优先冲突 | 架构污染 | 服务端对象独立权威源，客户端只读拉取 |

---

# 第十二章 最终产品效果

## 12.1 MVP 验收故事

1. 新用户首次启动，在无账号、无网络状态下创建目标并导入题库。
2. 用户完成一组选择题，关闭应用后重新打开，答案草稿和会话状态正确恢复。
3. 提交后立即看到客观题得分、解析、知识点和错题归档。
4. 到期复习自动出现在 Dashboard，用户可离线完成复习并更新间隔。
5. 用户请求 AI Tutor 时看到流式引导；取消请求后页面可继续使用。
6. 用户从本地资料提问，回答包含准确引用；无证据时明确提示。
7. 用户导出加密备份，在新工作区恢复后题目、记录、计划和设置完整。

### 12.1.1 扩展验收故事（V2.1 规划，🔜 P5–P8）

8. 用户将错题与资料要点生成闪卡，连续复习一周后到期队列按间隔自动排期，评级全部走服务端计算。
9. 用户按知识点与难度自动组卷并进入考试模式；倒计时结束自动提交，成绩报告与错题归档正确。
10. 用户完成一周学习后收到周报：总时长、正确率趋势、薄弱知识点与下周建议，可导出 PDF/JSON。
11. 用户使用番茄钟完成 4 轮专注，中断原因被记录；专注数据计入打卡与周报。
12. 用户开启复习提醒与免打扰时段：到期复习按时提醒，免打扰期间只保留考试提醒。
13. 家长经邀请码绑定学生后仅看到聚合报告与使用限制，无法打开 AI 会话原文与答案明细。
14. 教师发布作业，学生提交后客观题自动判分、主观题 AI 预批，教师确认后成绩回传；学生可申诉。
15. 管理员在审核工作台对 AI 生成题执行通过/打回，操作全部进入不可篡改审计日志。
16. 用户安装插件并授予权限后使用自定义导入解析器；禁用插件后主流程不受影响。
17. 用户在无网络环境完整完成闪卡复习、专注计时与本地问答；联网后事件幂等补写，无重复数据。

## 12.2 性能目标

| 项目 | 目标 |
|---|---:|
| 冷启动到可交互 | ≤ 3 秒（常规开发机） |
| 本地页面操作反馈 | P95 ≤ 100 ms |
| 本地题目提交 | P95 ≤ 300 ms，不含 AI |
| SQLite 常用查询 | P95 ≤ 100 ms |
| AI 首 Token | P95 ≤ 5 秒，受 Provider 影响 |
| 练习草稿恢复 | 重启后 100% 恢复最近一次已确认保存 |
| 数据恢复 | 测试备份恢复成功率 100% |

### 12.2.1 扩展性能目标（V2.1 规划）

| 项目 | 目标 |
|---|---:|
| 闪卡到期队列（10 万卡） | P95 ≤ 200 ms |
| 笔记全文搜索（1 万条） | P95 ≤ 200 ms |
| 报告聚合（10 万事件） | P95 ≤ 5 s（异步） |
| 组卷生成（5000 题库） | P95 ≤ 2 s |
| 知识图谱渲染（2000 节点） | 首帧 ≤ 1 s，Top-N 降级 |
| TTS 首句 | ≤ 1.5 s |
| 通知调度评估 | 每分钟轮询 ≤ 50 ms |
| 插件加载/禁用 | ≤ 500 ms，不影响主进程 |

## 12.3 安全与合规标准

- 密钥进入系统凭据库，不进入源码、配置导出、日志或崩溃报告。
- 用户可查看、导出、删除个人数据和长期记忆。
- 第三方 SDK、字体、模型和题库内容保留许可证及来源记录。
- AI 页面展示生成内容提示、资料来源和人工复核边界。
- 发布包进行签名，自动更新支持失败回滚。

## 12.4 发布阻断条件

出现以下任一情况不得发布：核心数据丢失或恢复失败；存在未修复的越权、密钥泄露、路径穿越或代码执行逃逸；题目版本和提交无法追溯；AI 结构化结果可直接绕过业务校验写库；核心流程存在 P0/P1 阻断缺陷。

## 12.5 最终产品定义

Lumo AI 的完成状态不是“拥有十二个 Agent”或“拥有大量页面”，而是用户可以在本地稳定完成以下闭环：

```text
学习目标 → 能力诊断 → 个性计划 → 有依据的练习推荐
→ 稳定答题与判分 → 可解释反馈 → 错题复习
→ 能力画像更新 → 下一轮学习计划
```

当本地功能、AI 辅导、资料检索、数据恢复和安全边界均达到本章标准后，学生 MVP 才具备发布条件；教师、管理、多设备同步和跨平台能力按前述里程碑逐步扩展。

---

# 附录 A：统一错误码

| 错误码 | 含义 | 是否可重试 |
|---|---|---|
| `INVALID_ARGUMENT` | 参数校验失败 | 否 |
| `UNAUTHORIZED` | 未登录或凭据失效 | 登录后 |
| `FORBIDDEN` | 无权限 | 否 |
| `NOT_FOUND` | 资源不存在 | 否 |
| `CONFLICT` | 版本或同步冲突 | 处理冲突后 |
| `INVALID_STATE` | 状态迁移非法 | 否 |
| `DATABASE_UNAVAILABLE` | 数据库不可用 | 是 |
| `PROVIDER_TIMEOUT` | 外部模型超时 | 是 |
| `PROVIDER_RATE_LIMITED` | 外部模型限流 | 延迟后 |
| `OUTPUT_INVALID` | 模型输出不符合 Schema | 是 |
| `IMPORT_FAILED` | 资料或题库导入失败 | 修正后 |
| `SANDBOX_LIMIT` | 代码执行超限 | 修改代码后 |
| `REQUEST_CANCELLED` | 流式请求被用户取消 | 重新发起 |
| `FEATURE_DISABLED` | 功能被开关/家长模式禁用 | 开启后 |
| `QUOTA_EXCEEDED` | 配额超限（闪卡限额/稍后读满/组卷上限等） | 清理后 |
| `EXAM_IN_PROGRESS` | 考试进行中不允许该操作 | 考试结束后 |
| `PLUGIN_ERROR` | 插件异常（加载/执行/权限） | 修复插件后 |
| `WEBHOOK_FAILED` | Webhook 投递失败 | 是（指数退避） |
| `SHARE_EXPIRED` | 分享链接过期或已撤销 | 重新生成 |
| `FAMILY_BOUND` | 家庭绑定冲突（已达上限/重复绑定） | 处理绑定后 |
| `REVIEW_REQUIRED` | 内容需人工审核才能发布/分享 | 审核通过后 |

> 补充：Web 传输层前端侧错误标记 `NETWORK`（无法连接本地服务）与 `STREAM_FAILED`（SSE 中断），`retryable` 均为 true。

# 附录 B：文档完成标准

- 每章包含目标、范围、流程、数据或接口契约、失败处理、非功能约束和验收标准。
- 每项 AI 能力都明确输入来源、工具权限、结构化输出、成本预算、延迟预算、评测集和降级路径。
- 每项同步能力都明确事实源、操作粒度、幂等性、冲突策略、隐私权限和恢复步骤。
- 每项 MVP 承诺都能关联到用户任务流、自动化测试或人工验收用例。
- 所有外部依赖均可替换、可禁用，并且不会阻塞本地基础学习闭环。

## 附录 B 扩展：V2.1 补全自检清单

- [x] 需求全景矩阵（1.9）覆盖全部需求方向，且每行可回溯到详设小节。
- [x] 新增 18 个业务模块（4.8–4.25）均有详设：4.8–4.24 按统一模板（定位/功能清单/规则/数据模型/交互/失败处理/验收标准），4.25 为补充需求清单式（X1–X14 + 验收标准）。
- [x] 扩展 Agent（5.2.1）与扩展 AI 能力（10.9–10.15）具备输入来源、结构化输出、评测口径与降级路径。
- [x] 扩展数据表（6.2.1）与索引（6.3.1）覆盖新模块，事件类表只追加。
- [x] 扩展页面/路由/store（7.1.1/7.6.1）与扩展服务（8.2.1）一一对应。
- [x] 扩展同步分类（9.3.1）为每个新对象定义冲突策略与权威源。
- [x] 路线（11.9–11.11）将新增需求映射到 P5A–P8 里程碑、优先级与风险。
- [x] 验收故事（12.1.1）、性能目标（12.2.1）与错误码（附录 A）覆盖新增能力。
